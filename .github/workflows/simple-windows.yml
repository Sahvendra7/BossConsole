name: Simple Windows Test

on:
  workflow_dispatch:

jobs:
  simple-test:
    runs-on: windows-2022
    steps:
      - name: Test
        run: echo "Testing Windows workflow"
      - name: Check secrets
        if: ${{ secrets.CODE_SIGNING_CERT_HOST != '' }}
        run: echo "Secrets are available"