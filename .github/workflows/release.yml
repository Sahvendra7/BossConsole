name: 🚀 Release Build

on:
  workflow_dispatch:
    inputs:
      version_increment:
        description: 'Version increment type'
        required: true
        default: 'patch'
        type: choice
        options:
          - patch
          - minor
          - major
      create_release:
        description: 'Create GitHub release'
        required: true
        default: true
        type: boolean
  push:
    tags:
      - 'v*.*.*'

env:
  GRADLE_OPTS: '-Dorg.gradle.daemon=false -Dorg.gradle.parallel=true -Dorg.gradle.caching=true'
  # Production credentials for release builds
  JXBROWSER_LICENSE_KEY: ${{ secrets.JXBROWSER_LICENSE_KEY }}
  SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  SUPABASE_FUNCTION_URL: ${{ secrets.SUPABASE_FUNCTION_URL }}

jobs:
  prepare-version:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get-version.outputs.version }}
      should-build: ${{ steps.check-changes.outputs.should-build }}
      updated-sha: ${{ steps.commit-version.outputs.updated-sha }}
      should-create-tag: ${{ steps.commit-version.outputs.should-create-tag }}
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.PUBLIC_REPO_TOKEN }}

      - name: 🔄 Increment Version (Manual Trigger)
        if: github.event_name == 'workflow_dispatch'
        run: |
          # Increment version based on input
          case "${{ github.event.inputs.version_increment }}" in
            "patch")
              ./gradlew incrementVersion
              ;;
            "minor")
              ./gradlew incrementMinor
              ;;
            "major")
              ./gradlew incrementMajor
              ;;
          esac
          
          # Get the new version
          NEW_VERSION=$(grep "^app.version=" version.properties | cut -d'=' -f2)
          echo "New version: $NEW_VERSION"

      - name: 📝 Commit Version Changes
        id: commit-version
        if: github.event_name == 'workflow_dispatch'
        run: |
          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Check if there are changes to commit
          if git diff --quiet version.properties; then
            echo "No version changes to commit"
            echo "updated-sha=${{ github.sha }}" >> $GITHUB_OUTPUT
          else
            # Commit version changes
            NEW_VERSION=$(grep "^app.version=" version.properties | cut -d'=' -f2)
            git add version.properties
            git commit -m "🔖 Bump version to $NEW_VERSION" \
                       -m "Auto-increment version for release build" \
                       -m "🤖 Automated by release workflow"

            # Push changes
            git push

            # DO NOT create tag here - will be created after successful builds
            # Get the new commit SHA
            NEW_SHA=$(git rev-parse HEAD)
            echo "updated-sha=$NEW_SHA" >> $GITHUB_OUTPUT
            echo "should-create-tag=true" >> $GITHUB_OUTPUT
            echo "✅ Version committed: $NEW_VERSION (tag will be created after builds)"
          fi

      - name: 📋 Get Version
        id: get-version
        run: |
          if [[ "${{ github.event_name }}" == "push" && "${{ github.ref }}" == refs/tags/* ]]; then
            # Extract version from tag (e.g., refs/tags/v8.11.2 -> 8.11.2)
            TAG_VERSION="${{ github.ref }}"
            TAG_VERSION="${TAG_VERSION#refs/tags/v}"
            echo "version=$TAG_VERSION" >> $GITHUB_OUTPUT
            echo "📦 Building version from tag: $TAG_VERSION"
            
            # Verify tag version matches version.properties
            PROPS_VERSION=$(grep "^app.version=" version.properties | cut -d'=' -f2)
            if [[ "$TAG_VERSION" != "$PROPS_VERSION" ]]; then
              echo "⚠️ Warning: Tag version ($TAG_VERSION) differs from version.properties ($PROPS_VERSION)"
              echo "Updating version.properties to match tag version for build consistency"
              
              # Parse tag version
              IFS='.' read -r TAG_MAJOR TAG_MINOR TAG_PATCH <<< "$TAG_VERSION"
              
              # Update version.properties to match tag
              sed -i "s/^app.version.major=.*/app.version.major=$TAG_MAJOR/" version.properties
              sed -i "s/^app.version.minor=.*/app.version.minor=$TAG_MINOR/" version.properties
              sed -i "s/^app.version.patch=.*/app.version.patch=$TAG_PATCH/" version.properties
              sed -i "s/^app.version=.*/app.version=$TAG_VERSION/" version.properties
              sed -i "s/^app.bundle.version=.*/app.bundle.version=$TAG_VERSION/" version.properties
              
              echo "✅ Updated version.properties to match tag: $TAG_VERSION"
            fi
          else
            # Use version from properties file
            VERSION=$(grep "^app.version=" version.properties | cut -d'=' -f2)
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "📦 Building version from properties: $VERSION"
          fi

      - name: 🔍 Check for Changes
        id: check-changes
        run: |
          # Build on both manual trigger and tag pushes
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "should-build=true" >> $GITHUB_OUTPUT
            echo "trigger=manual" >> $GITHUB_OUTPUT
          elif [[ "${{ github.event_name }}" == "push" && "${{ github.ref }}" == refs/tags/* ]]; then
            echo "should-build=true" >> $GITHUB_OUTPUT
            echo "trigger=tag" >> $GITHUB_OUTPUT
          else
            echo "should-build=false" >> $GITHUB_OUTPUT
            echo "trigger=none" >> $GITHUB_OUTPUT
          fi

  build-macos:
    needs: prepare-version
    if: needs.prepare-version.outputs.should-build == 'true'
    runs-on: macos-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.prepare-version.outputs.updated-sha }}

      - name: ☕ Setup JDK
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 🎯 Setup Gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-read-only: false

      - name: 🔧 Setup Build Environment
        shell: bash
        run: |
          echo "🔧 Setting up build environment for ${{ matrix.platform }}"
          echo "📦 Version: ${{ needs.prepare-version.outputs.version }}"
          
          # Verify version consistency
          ./gradlew showVersion

      # macOS specific setup
      - name: 🍎 Setup macOS Environment
        run: |
          # Install required dependencies for DMG creation
          brew install --quiet create-dmg || true
          
          # Verify macOS build tools
          xcode-select --print-path
          
          # Setup macOS code signing environment with P12 certificate
          if [[ -n "${{ secrets.MACOS_P12_CERTIFICATE }}" ]]; then
            echo "🔐 Setting up macOS code signing with P12 certificate..."
            
            # Decode and install P12 certificate
            echo "${{ secrets.MACOS_P12_CERTIFICATE }}" | base64 --decode > certificate.p12
            
            # Create temporary keychain
            security create-keychain -p temp_password temp_keychain
            security default-keychain -s temp_keychain
            security unlock-keychain -p temp_password temp_keychain
            
            # Set keychain settings
            security set-keychain-settings -lut 3600 temp_keychain
            
            # Import certificate
            security import certificate.p12 -k temp_keychain -P "${{ secrets.MACOS_P12_PASSWORD }}" -T /usr/bin/codesign -T /usr/bin/security
            
            # Allow codesign to access the key
            security set-key-partition-list -S apple-tool:,apple: -s -k temp_password temp_keychain
            
            # Ensure keychain is available for subsequent steps
            security list-keychains -s temp_keychain
            
            # Set up environment variables
            echo "DEVELOPER_ID=${{ secrets.MACOS_DEVELOPER_ID }}" >> $GITHUB_ENV
            echo "MACOS_DEVELOPER_ID=${{ secrets.MACOS_DEVELOPER_ID }}" >> $GITHUB_ENV
            echo "TEAM_ID=${{ secrets.MACOS_TEAM_ID }}" >> $GITHUB_ENV
            echo "APPLE_ID=${{ secrets.MACOS_APPLE_ID }}" >> $GITHUB_ENV
            echo "APP_PASSWORD=${{ secrets.MACOS_APP_PASSWORD }}" >> $GITHUB_ENV
            echo "DISABLE_MACOS_SIGNING=false" >> $GITHUB_ENV
            
            # Clean up certificate file
            rm certificate.p12
            
            # Verify certificate installation
            echo "🔍 Verifying certificate installation..."
            security find-identity -v -p codesigning temp_keychain
            
            echo "✅ macOS code signing configured with P12 certificate"
          else
            echo "⚠️ MACOS_P12_CERTIFICATE not found"
            echo "   Add your P12 certificate to GitHub secrets for production builds"
            echo "   Creating unsigned DMG for testing..."
            echo "DISABLE_MACOS_SIGNING=true" >> $GITHUB_ENV
          fi


      - name: 🏗️ Build macOS DMG
        env:
          DISABLE_MACOS_SIGNING: ${{ env.DISABLE_MACOS_SIGNING }}
        run: |
          echo "🏗️ Building macOS DMG with integrated PTY4J signing..."

          # Debug environment variables
          echo "DISABLE_MACOS_SIGNING: $DISABLE_MACOS_SIGNING"

          # Make gradlew executable
          chmod +x ./gradlew

          # The Gradle build now automatically handles PTY4J signing
          # The signPty4jBinaries task will run automatically when needed
          ./gradlew packageDmg

          echo "✅ macOS DMG build completed successfully"

      - name: 🔍 Verify Built Version (macOS)
        run: |
          echo "🔍 Verifying version inside built artifacts..."

          EXPECTED_VERSION="${{ needs.prepare-version.outputs.version }}"
          echo "Expected version: $EXPECTED_VERSION"

          # Check generated VersionConstants.kt
          GENERATED_FILE="composeApp/build/generated/source/version/ai/rever/boss/utils/VersionConstants.kt"
          if [[ -f "$GENERATED_FILE" ]]; then
            echo "Checking generated VersionConstants.kt..."

            MAJOR=$(grep "const val MAJOR =" "$GENERATED_FILE" | sed 's/.*= //;s/ .*//')
            MINOR=$(grep "const val MINOR =" "$GENERATED_FILE" | sed 's/.*= //;s/ .*//')
            PATCH=$(grep "const val PATCH =" "$GENERATED_FILE" | sed 's/.*= //;s/ .*//')

            BUILT_VERSION="${MAJOR}.${MINOR}.${PATCH}"
            echo "Built version (from VersionConstants.kt): $BUILT_VERSION"

            if [[ "$BUILT_VERSION" != "$EXPECTED_VERSION" ]]; then
              echo "❌ VERSION MISMATCH DETECTED!"
              echo "   Expected: $EXPECTED_VERSION"
              echo "   Built: $BUILT_VERSION"
              echo "   This is the root cause of Issue #111"
              echo ""
              echo "VersionConstants.kt contents:"
              cat "$GENERATED_FILE"
              exit 1
            else
              echo "✅ Version verification passed: $BUILT_VERSION"
            fi
          else
            echo "⚠️ VersionConstants.kt not found - this indicates build issue"
            exit 1
          fi

      # macOS notarization (only for signed DMGs)
      - name: 🍎 Notarize macOS DMG
        if: env.DISABLE_MACOS_SIGNING != 'true'
        run: |
          echo "🍎 Starting macOS notarization process..."

          # Find the DMG file
          DMG_PATH=$(find composeApp/build/compose/binaries/main/dmg -name "*.dmg" | head -1)

          if [[ -n "$DMG_PATH" && -f "$DMG_PATH" ]]; then
            echo "📦 Found DMG: $DMG_PATH"

            # Submit for notarization and capture the submission ID
            echo "🔐 Submitting DMG for notarization..."
            NOTARIZATION_OUTPUT=$(xcrun notarytool submit "$DMG_PATH" \
              --apple-id "$APPLE_ID" \
              --password "$APP_PASSWORD" \
              --team-id "${{ secrets.MACOS_TEAM_ID }}" \
              --wait 2>&1)

            NOTARIZATION_EXIT_CODE=$?
            echo "$NOTARIZATION_OUTPUT"

            # Extract submission ID from output
            SUBMISSION_ID=$(echo "$NOTARIZATION_OUTPUT" | grep "id:" | head -1 | awk '{print $2}')
            echo "📋 Submission ID: $SUBMISSION_ID"

            # Check if notarization succeeded
            if echo "$NOTARIZATION_OUTPUT" | grep -q "status: Accepted"; then
              echo "✅ Notarization accepted"

              # Staple the notarization
              echo "📎 Stapling notarization to DMG..."
              xcrun stapler staple "$DMG_PATH"

              echo "✅ macOS notarization completed successfully"
            else
              echo "❌ Notarization failed or returned non-Accepted status"

              # Fetch detailed log if we have a submission ID
              if [[ -n "$SUBMISSION_ID" ]]; then
                echo "📜 Fetching detailed notarization log..."
                xcrun notarytool log "$SUBMISSION_ID" \
                  --apple-id "$APPLE_ID" \
                  --password "$APP_PASSWORD" \
                  --team-id "${{ secrets.MACOS_TEAM_ID }}"
              fi

              exit 1
            fi
          else
            echo "❌ DMG file not found for notarization"
            exit 1
          fi

      # Skip notarization info for unsigned builds
      - name: 🍎 Skip Notarization (Unsigned Build)
        if: env.DISABLE_MACOS_SIGNING == 'true'
        run: |
          echo "ℹ️ Skipping notarization for unsigned DMG"
          echo "   This DMG is for development/testing only"
          echo "   For distribution, add proper code signing certificates"

      - name: 📊 Verify Build Artifacts
        run: |
          echo "📊 Verifying macOS DMG..."
          
          # Check if DMG exists
          if ls composeApp/build/compose/binaries/main/dmg/*.dmg 1> /dev/null 2>&1; then
            echo "✅ DMG found!"
            ls -la composeApp/build/compose/binaries/main/dmg/
          else
            echo "❌ DMG not found!"
            exit 1
          fi

      - name: 📤 Upload macOS DMG
        uses: actions/upload-artifact@v5
        with:
          name: boss-macos-${{ needs.prepare-version.outputs.version }}
          path: composeApp/build/compose/binaries/main/dmg/*.dmg
          retention-days: 30

  build-linux-amd64:
    needs: prepare-version
    if: needs.prepare-version.outputs.should-build == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.prepare-version.outputs.updated-sha }}

      - name: ☕ Setup JDK
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 🎯 Setup Gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-read-only: false

      - name: 🔧 Setup Build Environment
        run: |
          echo "🔧 Setting up build environment for Linux AMD64"
          echo "📦 Version: ${{ needs.prepare-version.outputs.version }}"

          # Verify version consistency
          ./gradlew showVersion

      - name: 🏗️ Build Linux Packages (AMD64)
        run: |
          echo "🏗️ Building Linux AMD64 packages..."

          # Create DEB and RPM packages
          ./gradlew :composeApp:packageDistributionForCurrentOS
          echo "✅ Linux native packages (DEB, RPM) created successfully"

          # Create executable JAR for universal Linux distribution
          ./gradlew :composeApp:createExecutableJar
          echo "✅ Executable JAR created successfully"

      - name: 🔍 Verify Built Version (Linux AMD64)
        run: |
          echo "🔍 Verifying version inside built artifacts..."

          EXPECTED_VERSION="${{ needs.prepare-version.outputs.version }}"
          echo "Expected version: $EXPECTED_VERSION"

          # Check generated VersionConstants.kt
          GENERATED_FILE="composeApp/build/generated/source/version/ai/rever/boss/utils/VersionConstants.kt"
          if [[ -f "$GENERATED_FILE" ]]; then
            echo "Checking generated VersionConstants.kt..."

            MAJOR=$(grep "const val MAJOR =" "$GENERATED_FILE" | sed 's/.*= //;s/ .*//')
            MINOR=$(grep "const val MINOR =" "$GENERATED_FILE" | sed 's/.*= //;s/ .*//')
            PATCH=$(grep "const val PATCH =" "$GENERATED_FILE" | sed 's/.*= //;s/ .*//')

            BUILT_VERSION="${MAJOR}.${MINOR}.${PATCH}"
            echo "Built version (from VersionConstants.kt): $BUILT_VERSION"

            if [[ "$BUILT_VERSION" != "$EXPECTED_VERSION" ]]; then
              echo "❌ VERSION MISMATCH DETECTED!"
              echo "   Expected: $EXPECTED_VERSION"
              echo "   Built: $BUILT_VERSION"
              echo "   This is the root cause of Issue #111"
              echo ""
              echo "VersionConstants.kt contents:"
              cat "$GENERATED_FILE"
              exit 1
            else
              echo "✅ Version verification passed: $BUILT_VERSION"
            fi
          else
            echo "⚠️ VersionConstants.kt not found - this indicates build issue"
            exit 1
          fi

      - name: 📊 Verify Linux Build Artifacts (AMD64)
        run: |
          echo "📊 Verifying Linux AMD64 packages..."

          # Check for DEB files
          if ls composeApp/build/compose/binaries/main/deb/*.deb 1> /dev/null 2>&1; then
            echo "✅ DEB package found!"
            ls -la composeApp/build/compose/binaries/main/deb/
          else
            echo "⚠️ DEB package not found"
          fi

          # Check for RPM files
          if ls composeApp/build/compose/binaries/main/rpm/*.rpm 1> /dev/null 2>&1; then
            echo "✅ RPM package found!"
            ls -la composeApp/build/compose/binaries/main/rpm/
          else
            echo "⚠️ RPM package not found"
          fi

          # Check for JAR files
          if ls composeApp/build/libs/*-all.jar 1> /dev/null 2>&1; then
            echo "✅ Executable JAR found!"
            ls -la composeApp/build/libs/*-all.jar
          else
            echo "⚠️ Executable JAR not found"
          fi

      - name: 📤 Upload Linux Packages (AMD64)
        uses: actions/upload-artifact@v5
        with:
          name: boss-linux-amd64-${{ needs.prepare-version.outputs.version }}
          path: |
            composeApp/build/compose/binaries/main/deb/
            composeApp/build/compose/binaries/main/rpm/
            composeApp/build/libs/*-all.jar
          retention-days: 30

  build-linux-arm64:
    needs: prepare-version
    if: needs.prepare-version.outputs.should-build == 'true'
    runs-on: ubuntu-24.04-arm
    continue-on-error: true  # Graceful failure if ARM runners unavailable

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.prepare-version.outputs.updated-sha }}

      - name: ☕ Setup JDK
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 🎯 Setup Gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-read-only: false

      - name: 🔧 Setup Build Environment
        run: |
          echo "🔧 Setting up build environment for Linux ARM64"
          echo "📦 Version: ${{ needs.prepare-version.outputs.version }}"

          # Verify version consistency
          ./gradlew showVersion

      - name: 🏗️ Build Linux Packages (ARM64)
        run: |
          echo "🏗️ Building Linux ARM64 packages..."

          # Create DEB and RPM packages
          ./gradlew :composeApp:packageDistributionForCurrentOS
          echo "✅ Linux native packages (DEB, RPM) created successfully"

          # Create executable JAR for ARM64 Linux distribution
          ./gradlew :composeApp:createExecutableJar
          echo "✅ Executable JAR created successfully"

      - name: 🔍 Verify Built Version (Linux ARM64)
        run: |
          echo "🔍 Verifying version inside built artifacts..."

          EXPECTED_VERSION="${{ needs.prepare-version.outputs.version }}"
          echo "Expected version: $EXPECTED_VERSION"

          # Check generated VersionConstants.kt
          GENERATED_FILE="composeApp/build/generated/source/version/ai/rever/boss/utils/VersionConstants.kt"
          if [[ -f "$GENERATED_FILE" ]]; then
            echo "Checking generated VersionConstants.kt..."

            MAJOR=$(grep "const val MAJOR =" "$GENERATED_FILE" | sed 's/.*= //;s/ .*//')
            MINOR=$(grep "const val MINOR =" "$GENERATED_FILE" | sed 's/.*= //;s/ .*//')
            PATCH=$(grep "const val PATCH =" "$GENERATED_FILE" | sed 's/.*= //;s/ .*//')

            BUILT_VERSION="${MAJOR}.${MINOR}.${PATCH}"
            echo "Built version (from VersionConstants.kt): $BUILT_VERSION"

            if [[ "$BUILT_VERSION" != "$EXPECTED_VERSION" ]]; then
              echo "❌ VERSION MISMATCH DETECTED!"
              echo "   Expected: $EXPECTED_VERSION"
              echo "   Built: $BUILT_VERSION"
              echo "   This is the root cause of Issue #111"
              echo ""
              echo "VersionConstants.kt contents:"
              cat "$GENERATED_FILE"
              exit 1
            else
              echo "✅ Version verification passed: $BUILT_VERSION"
            fi
          else
            echo "⚠️ VersionConstants.kt not found - this indicates build issue"
            exit 1
          fi

      - name: 📊 Verify Linux Build Artifacts (ARM64)
        run: |
          echo "📊 Verifying Linux ARM64 packages..."

          # Check for DEB files
          if ls composeApp/build/compose/binaries/main/deb/*.deb 1> /dev/null 2>&1; then
            echo "✅ DEB package found!"
            ls -la composeApp/build/compose/binaries/main/deb/
          else
            echo "⚠️ DEB package not found"
          fi

          # Check for RPM files
          if ls composeApp/build/compose/binaries/main/rpm/*.rpm 1> /dev/null 2>&1; then
            echo "✅ RPM package found!"
            ls -la composeApp/build/compose/binaries/main/rpm/
          else
            echo "⚠️ RPM package not found"
          fi

          # Check for JAR files
          if ls composeApp/build/libs/*-all.jar 1> /dev/null 2>&1; then
            echo "✅ Executable JAR found!"
            ls -la composeApp/build/libs/*-all.jar
          else
            echo "⚠️ Executable JAR not found"
          fi

      - name: 📤 Upload Linux Packages (ARM64)
        uses: actions/upload-artifact@v5
        with:
          name: boss-linux-arm64-${{ needs.prepare-version.outputs.version }}
          path: |
            composeApp/build/compose/binaries/main/deb/
            composeApp/build/compose/binaries/main/rpm/
            composeApp/build/libs/*-all.jar
          retention-days: 30

  build-windows:
    needs: prepare-version
    if: needs.prepare-version.outputs.should-build == 'true'
    runs-on: windows-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.prepare-version.outputs.updated-sha }}

      - name: ☕ Setup JDK
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: 🎯 Setup Gradle
        uses: gradle/actions/setup-gradle@v5

      - name: 🔧 Setup Build Environment
        run: |
          echo "🔧 Setting up build environment for Windows"
          echo "📦 Version: ${{ needs.prepare-version.outputs.version }}"
          
          # Verify version consistency
          ./gradlew showVersion

      - name: 🏗️ Build Windows MSI
        run: |
          echo "🏗️ Building Windows MSI..."

          # Make gradlew executable and build
          ./gradlew packageMsi

          echo "✅ Windows MSI build completed successfully"

      - name: 🔍 Verify Built Version (Windows)
        run: |
          echo "🔍 Verifying version inside built artifacts..."

          EXPECTED_VERSION="${{ needs.prepare-version.outputs.version }}"
          echo "Expected version: $EXPECTED_VERSION"

          # Check generated VersionConstants.kt
          GENERATED_FILE="composeApp/build/generated/source/version/ai/rever/boss/utils/VersionConstants.kt"
          if [ -f "$GENERATED_FILE" ]; then
            echo "Checking generated VersionConstants.kt..."

            MAJOR=$(grep "const val MAJOR =" "$GENERATED_FILE" | sed 's/.*= //;s/ .*//')
            MINOR=$(grep "const val MINOR =" "$GENERATED_FILE" | sed 's/.*= //;s/ .*//')
            PATCH=$(grep "const val PATCH =" "$GENERATED_FILE" | sed 's/.*= //;s/ .*//')

            BUILT_VERSION="${MAJOR}.${MINOR}.${PATCH}"
            echo "Built version (from VersionConstants.kt): $BUILT_VERSION"

            if [ "$BUILT_VERSION" != "$EXPECTED_VERSION" ]; then
              echo "❌ VERSION MISMATCH DETECTED!"
              echo "   Expected: $EXPECTED_VERSION"
              echo "   Built: $BUILT_VERSION"
              echo "   This is the root cause of Issue #111"
              echo ""
              echo "VersionConstants.kt contents:"
              cat "$GENERATED_FILE"
              exit 1
            else
              echo "✅ Version verification passed: $BUILT_VERSION"
            fi
          else
            echo "⚠️ VersionConstants.kt not found - this indicates build issue"
            exit 1
          fi
        shell: bash

      - name: Check if signing secret is available
        id: check-secret
        run: |
          if [ -n "$CERT_HOST" ]; then
            echo "can_sign=true" >> $GITHUB_OUTPUT
          else
            echo "can_sign=false" >> $GITHUB_OUTPUT
          fi
        shell: bash
        env:
          CERT_HOST: ${{ secrets.CODE_SIGNING_CERT_HOST }}

      - name: Find MSI File
        id: find-msi
        run: |
          $msiFiles = Get-ChildItem "composeApp/build/compose/binaries/main/msi/BOSS-*.msi" -ErrorAction SilentlyContinue
          if ($msiFiles) {
            $msiPath = $msiFiles[0].FullName
            Write-Host "Found MSI: $msiPath"
            echo "msi-path=$msiPath" >> $env:GITHUB_OUTPUT
          } else {
            Write-Host "MSI file not found!"
            exit 1
          }
        shell: powershell

      - name: Decode Client Certificate
        if: ${{ steps.check-secret.outputs.can_sign == 'true' }}
        run: |
          try {
            $certContent = "${{ secrets.CODE_SIGNING_CLIENT_CERT }}"
            $certBytes = [System.Convert]::FromBase64String($certContent)
            $certPath = "${{ runner.temp }}\Certificate_pkcs12.p12"
            [System.IO.File]::WriteAllBytes($certPath, $certBytes)
            Write-Host "Client cert decoded successfully to $certPath"
          } catch {
            Write-Host "Error decoding client cert: $_"
            exit 1
          }
        shell: powershell

      - name: Verify Client Cert File Exists
        if: ${{ steps.check-secret.outputs.can_sign == 'true' }}
        run: |
          $certPath = "${{ runner.temp }}\Certificate_pkcs12.p12"
          if (Test-Path $certPath) {
            Write-Host "[OK] Cert file exists at $certPath"
            Write-Host "File size: $((Get-Item $certPath).Length) bytes"
          } else {
            Write-Host "[ERROR] Cert file MISSING at $certPath"
            exit 1
          }
        shell: powershell

      - name: Install DigiCert STM Client Tools
        if: ${{ steps.check-secret.outputs.can_sign == 'true' }}
        uses: digicert/ssm-code-signing@v1.1.1
        env:
          SM_HOST: ${{ secrets.CODE_SIGNING_CERT_HOST }}
          SM_API_KEY: ${{ secrets.CODE_SIGNING_CERT_HOST_API_KEY }}
          SM_CLIENT_CERT_FILE: ${{ runner.temp }}\Certificate_pkcs12.p12
          SM_CLIENT_CERT_PASSWORD: ${{ secrets.CODE_SIGNING_CLIENT_CERT_PASSWORD }}
          SM_TRACE: true

      - name: Reset SMCTL Credentials
        if: ${{ steps.check-secret.outputs.can_sign == 'true' }}
        run: |
          smctl credentials delete
          smctl credentials save ${{ secrets.CODE_SIGNING_CERT_HOST_API_KEY }} ${{ secrets.CODE_SIGNING_CLIENT_CERT_PASSWORD }}
        shell: cmd
        env:
          SM_HOST: ${{ secrets.CODE_SIGNING_CERT_HOST }}
          SM_API_KEY: ${{ secrets.CODE_SIGNING_CERT_HOST_API_KEY }}
          SM_CLIENT_CERT_FILE: ${{ runner.temp }}\Certificate_pkcs12.p12
          SM_CLIENT_CERT_PASSWORD: ${{ secrets.CODE_SIGNING_CLIENT_CERT_PASSWORD }}
          SM_TRACE: true

      - name: Run SMCTL Healthcheck
        if: ${{ steps.check-secret.outputs.can_sign == 'true' }}
        run: |
          smctl healthcheck
        shell: cmd
        env:
          SM_HOST: ${{ secrets.CODE_SIGNING_CERT_HOST }}
          SM_API_KEY: ${{ secrets.CODE_SIGNING_CERT_HOST_API_KEY }}
          SM_CLIENT_CERT_FILE: ${{ runner.temp }}\Certificate_pkcs12.p12
          SM_CLIENT_CERT_PASSWORD: ${{ secrets.CODE_SIGNING_CLIENT_CERT_PASSWORD }}
          SM_TRACE: true

      - name: Sign Windows MSI with DigiCert KeyLocker
        if: ${{ steps.check-secret.outputs.can_sign == 'true' }}
        run: |
          echo "Signing MSI: ${{ steps.find-msi.outputs.msi-path }}"
          echo "Using keypair: ${{ secrets.CODE_SIGNING_KEYPAIR_ALIAS }}"
          smctl sign --keypair-alias "${{ secrets.CODE_SIGNING_KEYPAIR_ALIAS }}" --input "${{ steps.find-msi.outputs.msi-path }}" --verbose
        shell: bash
        env:
          SM_HOST: ${{ secrets.CODE_SIGNING_CERT_HOST }}
          SM_API_KEY: ${{ secrets.CODE_SIGNING_CERT_HOST_API_KEY }}
          SM_CLIENT_CERT_FILE: ${{ runner.temp }}\Certificate_pkcs12.p12
          SM_CLIENT_CERT_PASSWORD: ${{ secrets.CODE_SIGNING_CLIENT_CERT_PASSWORD }}
          SM_TRACE: true

      - name: Check SMKSP Log (for diagnostics)
        if: always()  # Runs even if signing fails
        run: |
          $logPath = "$env:USERPROFILE\.signingmanager\logs\smksp.log"
          if (Test-Path $logPath) {
            Write-Host "SMKSP Log Contents:"
            Get-Content $logPath -Tail 50  # Last 50 lines for recent errors
          } else {
            Write-Host "SMKSP log not found."
          }
        shell: powershell

      - name: 📊 Verify Build Artifacts
        run: |
          $msiPath = "composeApp/build/compose/binaries/main/msi/BOSS-*.msi"
          $msiFiles = Get-ChildItem $msiPath -ErrorAction SilentlyContinue
          if ($msiFiles) {
            Write-Host "✅ MSI found: $($msiFiles[0].FullName)"
            Write-Host "   Size: $([math]::Round($msiFiles[0].Length/1MB,2)) MB"
          } else {
            Write-Host "❌ MSI not found at expected location"
            Write-Host "Searching for MSI files recursively..."
            $allMsi = Get-ChildItem "composeApp/build" -Recurse -Filter "*.msi" -ErrorAction SilentlyContinue
            if ($allMsi) {
              Write-Host "Found MSI at: $($allMsi[0].FullName)"
            } else {
              Write-Host "No MSI files found anywhere in build directory"
              exit 1
            }
          }
        shell: powershell

      - name: 📤 Upload Windows MSI
        uses: actions/upload-artifact@v5
        with:
          name: boss-windows-${{ needs.prepare-version.outputs.version }}
          path: composeApp/build/compose/binaries/main/msi/
          retention-days: 30

  create-release:
    needs: [prepare-version, build-macos, build-linux-amd64, build-linux-arm64, build-windows]
    if: needs.prepare-version.outputs.should-build == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v6
        with:
          ref: ${{ needs.prepare-version.outputs.updated-sha }}

      - name: 📥 Download All Artifacts
        uses: actions/download-artifact@v6
        with:
          path: ./artifacts

      - name: 📋 Prepare Release Assets
        run: |
          echo "📋 Preparing release assets..."
          mkdir -p ./release-assets

          # Copy and rename artifacts with consistent naming
          VERSION="${{ needs.prepare-version.outputs.version }}"

          # macOS Universal
          find ./artifacts -name "*.dmg" -exec cp {} ./release-assets/BOSS-${VERSION}-Universal.dmg \; 2>/dev/null || true

          # Windows
          find ./artifacts -name "*.msi" -exec cp {} ./release-assets/BOSS-${VERSION}.msi \; 2>/dev/null || true

          # Linux AMD64 artifacts (use path matching)
          find ./artifacts -name "*.deb" -path "*amd64*" -exec cp {} ./release-assets/BOSS-${VERSION}-amd64.deb \; 2>/dev/null || true
          find ./artifacts -name "*.rpm" -path "*amd64*" -exec cp {} ./release-assets/BOSS-${VERSION}-amd64.rpm \; 2>/dev/null || true
          find ./artifacts -name "*-all.jar" -path "*amd64*" -exec cp {} ./release-assets/BOSS-${VERSION}-amd64.jar \; 2>/dev/null || true

          # Linux ARM64 artifacts (tolerate missing - ARM64 build may fail)
          find ./artifacts -name "*.deb" -path "*arm64*" -exec cp {} ./release-assets/BOSS-${VERSION}-arm64.deb \; 2>/dev/null || true
          find ./artifacts -name "*.rpm" -path "*arm64*" -exec cp {} ./release-assets/BOSS-${VERSION}-arm64.rpm \; 2>/dev/null || true
          find ./artifacts -name "*-all.jar" -path "*arm64*" -exec cp {} ./release-assets/BOSS-${VERSION}-arm64.jar \; 2>/dev/null || true

          # List final artifacts
          echo "📦 Release assets:"
          ls -la ./release-assets/

      - name: 📝 Generate Release Notes
        id: release-notes
        run: |
          VERSION="${{ needs.prepare-version.outputs.version }}"

          # Generate release notes
          cat > release-notes.md << EOF
          # 🚀 BOSS $VERSION

          ## 📦 Downloads

          | Platform | Architecture | Package |
          |----------|--------------|---------|
          | **macOS** | Universal (Apple Silicon & Intel) | \`BOSS-${VERSION}-Universal.dmg\` |
          | **Windows** | x64 | \`BOSS-${VERSION}.msi\` |
          | **Linux DEB** | AMD64 (x86_64) | \`BOSS-${VERSION}-amd64.deb\` |
          | **Linux DEB** | ARM64 (aarch64) | \`BOSS-${VERSION}-arm64.deb\` |
          | **Linux RPM** | AMD64 (x86_64) | \`BOSS-${VERSION}-amd64.rpm\` |
          | **Linux RPM** | ARM64 (aarch64) | \`BOSS-${VERSION}-arm64.rpm\` |
          | **Linux JAR** | AMD64 (x86_64) | \`BOSS-${VERSION}-amd64.jar\` |
          | **Linux JAR** | ARM64 (aarch64) | \`BOSS-${VERSION}-arm64.jar\` |

          ## ✨ What's New

          - Auto-generated release for version $VERSION
          - Built with centralized version management system
          - All platforms synchronized and tested
          - Full Linux support with native packages (AMD64 and ARM64)

          ## 🔧 System Requirements

          - **macOS**: macOS 11.0+ (Big Sur)
          - **Windows**: Windows 10+ (64-bit)
          - **Linux**: Any modern distribution (AMD64 or ARM64)

          ## 🛠️ Installation

          ### macOS
          1. Download \`BOSS-${VERSION}-Universal.dmg\`
          2. Open the DMG and drag BOSS to Applications
          3. Launch from Applications folder

          ### Windows (64-bit)
          1. Download \`BOSS-${VERSION}.msi\`
          2. Run the installer as Administrator
          3. Launch from Start Menu or Desktop

          ### Linux (AMD64 / x86_64)

          **Ubuntu/Debian:**
          \`\`\`bash
          wget https://github.com/${{ github.repository }}/releases/download/v${VERSION}/BOSS-${VERSION}-amd64.deb
          sudo dpkg -i BOSS-${VERSION}-amd64.deb
          sudo apt-get install -f  # Fix any missing dependencies
          \`\`\`

          **RHEL/Fedora/openSUSE:**
          \`\`\`bash
          wget https://github.com/${{ github.repository }}/releases/download/v${VERSION}/BOSS-${VERSION}-amd64.rpm
          sudo rpm -i BOSS-${VERSION}-amd64.rpm
          \`\`\`

          **JAR (requires Java 17+):**
          \`\`\`bash
          wget https://github.com/${{ github.repository }}/releases/download/v${VERSION}/BOSS-${VERSION}-amd64.jar
          java -jar BOSS-${VERSION}-amd64.jar
          \`\`\`

          ### Linux (ARM64 / aarch64)

          **Ubuntu/Debian:**
          \`\`\`bash
          wget https://github.com/${{ github.repository }}/releases/download/v${VERSION}/BOSS-${VERSION}-arm64.deb
          sudo dpkg -i BOSS-${VERSION}-arm64.deb
          sudo apt-get install -f  # Fix any missing dependencies
          \`\`\`

          **RHEL/Fedora/openSUSE:**
          \`\`\`bash
          wget https://github.com/${{ github.repository }}/releases/download/v${VERSION}/BOSS-${VERSION}-arm64.rpm
          sudo rpm -i BOSS-${VERSION}-arm64.rpm
          \`\`\`

          **JAR (requires Java 17+):**
          \`\`\`bash
          wget https://github.com/${{ github.repository }}/releases/download/v${VERSION}/BOSS-${VERSION}-arm64.jar
          java -jar BOSS-${VERSION}-arm64.jar
          \`\`\`

          ---

          🤖 *This release was automatically built and published by GitHub Actions*
          EOF

          # Set output for release creation
          echo "notes-file=release-notes.md" >> $GITHUB_OUTPUT

      - name: 🎉 Create GitHub Release
        if: github.event.inputs.create_release != 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.prepare-version.outputs.version }}
          name: 🚀 BOSS ${{ needs.prepare-version.outputs.version }}
          body_path: ${{ steps.release-notes.outputs.notes-file }}
          files: ./release-assets/*
          draft: false
          prerelease: false
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.PUBLIC_REPO_TOKEN }}

      - name: 📢 Release Summary
        run: |
          echo "## 🎉 Release Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Version**: ${{ needs.prepare-version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Trigger**: ${{ github.event_name }}" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Platforms Built**: macOS, Linux (AMD64 + ARM64), Windows" >> $GITHUB_STEP_SUMMARY
          echo "✅ **Release Created**: https://github.com/${{ github.repository }}/releases/tag/v${{ needs.prepare-version.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Artifacts" >> $GITHUB_STEP_SUMMARY
          echo "- 🍎 macOS DMG (Universal - Apple Silicon & Intel)" >> $GITHUB_STEP_SUMMARY
          echo "- 🐧 Linux DEB (AMD64 + ARM64)" >> $GITHUB_STEP_SUMMARY
          echo "- 🐧 Linux RPM (AMD64 + ARM64)" >> $GITHUB_STEP_SUMMARY
          echo "- 🐧 Linux JAR (AMD64 + ARM64)" >> $GITHUB_STEP_SUMMARY
          echo "- 🪟 Windows MSI (x64)" >> $GITHUB_STEP_SUMMARY
