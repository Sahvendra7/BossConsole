# BOSS Release Notes

This directory contains detailed release notes for each version of BOSS.

## Quick Links

- [Latest Release](https://github.com/risa-labs-inc/BossConsole-Releases/releases/latest)
- [All Releases](https://github.com/risa-labs-inc/BossConsole-Releases/releases)

## Release Index

<!-- RELEASE_INDEX_START -->
| Version | Date | Summary |
|---------|------|---------|
| [v9.4.12](v9.4.12.md) | 2026-08-15 | A sign-in release. The auth card spanned the whole window: `AuthCard` wrote `Modifier.fillMaxWidth().widthIn(max = 400.dp)`, and the cap was silently discarded, because `fillMaxWidth` measures its child with `minWidth == maxWidth == the parent's width` and `widthIn` enforces incoming constraints, so it coerced its own 0..400 range back up into that fixed range and returned the parent width - the same rule `DialogCardWidthConventionTest` already guards, which misses this one because `AuthCard` is not a `BossDialog` call site. Nothing centred the column either, so capping the width alone would have shipped a 400.dp card against the left edge. An empty form also showed a field with no button at all: Continue was hidden until the text contained an `@`, leaving the card with nothing to act on, a 64.dp gap where the button belonged, and a form that changed height as you typed - it is always rendered now, disabled until the address is worth submitting, on one shared predicate rather than four inline copies. Both defects lived in shared code, so all four auth screens carried them, and `AuthScaffold` now replaces the frame those screens repeated verbatim - which is how three had a `verticalScroll` and the fourth, the one whose content grows with the number of registered passkeys, did not. The space the card was wrongly filling becomes a brand panel: drawn art from bossconsole.ai's visual language through the design system, with a bundled offline copy of the site's hero over it (local rather than the live URL, since a network fetch on the one screen a user cannot get past makes a blank pane likeliest exactly when the network is bad), a digit rain, orbiting capability chips with previews, a zoom-reveal entrance and a synthesised arrival swell - all of it independently guarded, so a disabled flag, a missing engine, a failed render or an absent script lands on the art. Review found the panel could take the sign-in screen down with it in three ways: `FluckEngine.engine.newBrowser()` ran on the composition dispatcher, a multi-second boot under a lock that froze the screen and is not covered by the startup prewarm, which returns early on the first-ever launch that shows this screen; `catch (e: Exception)` let `UnsatisfiedLinkError` through, the characteristic failure of a mismatched Chromium bundle; and `BrowserViewState` construction sat outside the catch entirely. Also caught: a leaked browser keyed on a value null until the recomposition after creation, a `LazyColumn` nested in the scaffold's scroll on the passkey screen, float drift dropping the grid's emphasis lines, a canvas at `z-index: -3` that had never once painted behind its parent's background, digits that never faded because 8-bit compositing rounds a faint pixel's wash to zero, and an entrance sound silent since the day it was written, because `pointermove` is not a user-activation gesture in Chromium (#181). **macOS 13.0+ required**, unchanged from v9.4.0. |
| [v9.4.11](v9.4.11.md) | 2026-08-15 | A plugin-platform release. The AI Gateway could not be updated or removed from the Toolbox - Update cleared the spinner and changed nothing, with no error anywhere. `checkCanUnload` counted any plugin that named the target in its manifest dependencies, including declarations marked `optional: true`, and jupyter-notebook, flow-tab and llmrpa all declare the gateway optional, so between them they vetoed its unload permanently; because the Toolbox updates a plugin by uninstalling and reinstalling it, the veto landed on the Update button. The same declaration was already being read the other way at install time, where the prompt words it as "works without it, but some of its features need it", so `blockingDependentsOf` now answers the unload question next to `missingFor` and counts only non-optional declarations. A *disabled* dependent stops vetoing too - `disablePlugin` never calls `pluginLoader.unloadPlugin`, so a disabled plugin is still in `getLoadedPlugins()` and was refusing on behalf of something that is not running - and the refusal is finally visible, where the delegate's `Boolean` return had dropped the reasons naming the plugins in the way and left the log stopping mid-sequence (#178). A plugin can now open another plugin's panel as a main-area tab through `SplitViewOperations.openPanelAsTab` (api 1.0.77): the host has done this for itself for a while, but every piece of it was internal, so a plugin could only reveal a panel in the sidebar and the Toolbox reached the panel-host constructor reflectively and had to accept losing the panel's state. It routes to the existing `requestPromoteToTab` rather than reimplementing the promote, which is what makes it a move - the cached component carries the panel's state across - and resolves the incoming `PanelId` against the registry rather than by data-class equality, since `PanelId` carries a `defaultOrder` a plugin has no way to look up and the api's own `AiAvailability` guesses it (#179). **macOS 13.0+ required**, unchanged from v9.4.0. |
| [v9.4.10](v9.4.10.md) | 2026-08-15 | A Windows and dialogs release. Installing an update on Windows worked and then simply ended: BOSS quit, msiexec ran, and nothing came back, because relaunching is entirely the helper script's job and the Windows one finished with `del "%~f0"` where macOS has always finished with `open "<app>"` - and there was nothing to relaunch with either, since `getCurrentApplicationPath` only resolved a macOS `.app` bundle. The launcher is now resolved while BOSS is still running (after msiexec there is no process left to ask), from `jpackage.app-path` first and then a walk up from the running jar that must find an `app\` or `runtime\` marker, since the ancestors of a per-user install are all user-writable. Three defects in the same script came with it: `/norestart` makes msiexec report 3010 whenever a file was in use, so a completed install was called a failure; `start` takes its first quoted token as a window *title*, so ProcessBuilder's quoting meant the helper never ran at all for an account whose name has a space; and every `timeout /t` was failing outright under redirected stdin, turning the quit poll into a hot loop hammering tasklist. Note the helper is generated by the *old* app, so the relaunch first appears on the update after this one (#174). Every window but the main one wore the JDK coffee cup on Windows - jpackage's icon goes on the launcher `.exe` and never reaches a live AWT window, and exactly one of fifteen window sites passed one, invisible on macOS and Linux which read the bundle and the `.desktop` file (#176). The Confirm Logout dialog rendered as a band across the whole window: `BossDialog`'s card is meant to be intrinsically sized, and the lightweight path caps a filling card at the platform default width while the heavyweight path the main window uses ignores that cap, so the bug shipped looking fine from Settings (#175). And installing Flow failed on every shipped build with "Tool not found in repository" for a row that is present and valid - a required `versionRange` that nobody reads hard-failed the decode of the whole detail response for one legacy-shaped dependency entry, and `getPlugin` flattened every repository's `Result` with `getOrNull()`, so the real error was destroyed before the wizard saw it and the same swallow was found on four more paths (#175). Also: the sidebar header's controls sit flush right again, where a two-weight layout laid the title's unused half *after* them (#173). **macOS 13.0+ required**, unchanged from v9.4.0. |
| [v9.4.9](v9.4.9.md) | 2026-08-15 | A plugin-reliability release. Closing a laptop lid was enough to disable every plugin at once: the watchdog measured heartbeat age against the wall clock, so a 48-second suspend aged every plugin's heartbeat by 48 seconds and convicted 24 of them inside 6ms, while its own check loop had been frozen for the same 48 seconds - proof the plugins were never at fault, and a signal that was discarded. What the "restart" then did was worse than the false positive: it replaced the sandbox scope while every component built in `register()` went on holding the cancelled one, and a cancelled scope swallows `launch()` without running it and without throwing, so the plugin went quietly inert behind a toast reporting success. The watchdog now discards any tick that overran, the plugin-facing scope is created once and re-armed rather than replaced, `restartAttempts` survives a restart so `maxRestartAttempts` is reachable at all, and the manager re-runs `register()` after a restart - without which the Toolbox's realtime channel, terminal-tab's approval collector and analytics' pipes stayed closed on a plugin that looked alive. Six review rounds on paths reaching the same looks-alive-does-nothing state from the other side: the error boundary's Restart button notified no listener and banked an attempt per press, a cancelled restart stranded the sandbox in `RESTARTING` (the one state the watchdog skips and the boundary shows no fallback for), a chronically slow host re-armed stall suppression for ever, and a throwing listener completed the watchdog's own coroutine (#171). A panel now says which build of a plugin it is running - the manifest version cannot, since a local jar and the released one carry the same `version` and an in-place hot reload rewrites neither the manifest nor `installed.json` - reading `1.0.3-debug` for bytes the store never vetted and `1.0.3-debug+<millis>` once they were replaced under the running plugin, from a `.sig` sidecar and an mtime that were already on disk. Clicking it offers the store build; the overflow menu can uninstall, where `removeInstalledPlugin` had zero production callers and every uninstall left a row pointing at a file that may be gone; and "Restart Panel" is gone, since it only recreated the panel component from the already-registered factory and looked like it picked up a new jar (#169). The focus-mode quick actions move into the right rail when there is one, which is the default configuration on Windows and the platform the floating cluster was built for, and Settings opens or raises instead of being a dead button - every affordance assigned `showSettingsDialog = true`, and assigning true to a flag that is already true changes nothing (#172). **macOS 13.0+ required**, unchanged from v9.4.0. |
| [v9.4.8](v9.4.8.md) | 2026-08-11 | Two reachability fixes and a repo boundary. Focus mode used to strand Settings, Search and Sign Out: those three actions live in `BossTopRightBar` and nowhere else, and the documented way back - hovering the top edge - is driven by Compose pointer events on an edge strip, which cannot fire over a browser tab, because in `HARDWARE_ACCELERATED` mode Chromium owns a foreign native window that composites over the Compose scene. That is why auto-reveal ships off on Windows, and it left focus mode plus a browser tab with no route to these actions except the menu bar. An icon-only cluster now pins to the bottom-right through `OverlayCorner`, for the same reason the hover strip fails, with the two `ToastOverlay` guards a non-focusable always-on-top AWT window needs: nothing composed while the top bar shows, and an unfocused window drawn in place rather than holding a dead click region. Review took out a `withFrameNanos` bounds poll that kept the Compose scene from ever going idle, and pinned the `hides(TOP) && !showTopBar` conjunction whose first half looks redundant and is what keeps the cluster off the launch path (#161). A plugin tab type declaring no input now opens on the first click instead of showing an empty "(optional)" field - a heuristic over three `NewTabSpec` fields, acceptable only because the api defaults make instant open unreachable by omission, with three review rounds on paths that were worse than the bug: a refused open was a complete no-op, a double-clicked tile opened two tabs, and gating the head of an if/else-if chain fell through to a focused URL field beside a "Play" button that discards what is typed (#167). The RISA LLM gateway moves to its own private repository - it ships in no BOSS release and CI references it nowhere, yet this repo is public, so every infra change to it was a public one - leaving the app's half of the contract and `docs/llm-gateway-contract.md` to stop the two sides drifting silently (#168). Also: five `[HOST-DEBUG]` printlns on the navigation-target path route through `BossLogger` (#160), and Kotlin 2.4.10, ktor 3.5.2, androidx-lifecycle 2.11.0 and netty 4.2.17.Final. **macOS 13.0+ required**, unchanged from v9.4.0. |
| [v9.4.7](v9.4.7.md) | 2026-08-10 | The Secret Manager becomes what it always was underneath: a per-user credential vault. `secret.read` reaches the baseline `user` role, so the panel opens for anyone signed in - a correction rather than a widening, since every secret RPC is already granted to `authenticated` with no permission check, each self-scopes with `auth.uid()`, and all four RLS policies on `public.secrets` are `auth.uid() = user_id`. The permission was a client-side visibility gate over a store that has always been per-user, translated forward verbatim from the pre-RBAC `requiresAdmin` flag, and granting it widens server-side access by zero rows. What it unblocks is AI: since provider settings moved into the secret-manager plugin, `DynamicPluginManager` skipping `register()` for an inaccessible plugin meant a non-admin got no AI provider settings, no way to add their own key, and a null `PluginContext.llmProvider` in every other plugin, so all AI in BOSS was admin-only by accident. The tightening that has to come with it: `share_secret` gated role targets on `can_manage_secret` alone, so any owner could share a credential with any global role - including `user`, a descendant of everything and therefore every account in the deployment, with no undo - which was survivable only while non-admins could not reach the panel. Role targets now require a new `secret.share.role`, checked before the role lookup so the error is not a role-existence oracle, and kept off the org-grantable allowlist so an org admin cannot mint a sharer of global roles; user and organisation targets are unchanged. Also: `Settings > AI Providers` names the permission to ask an admin for instead of reporting a loading state that will never resolve, and Secret Manager moves out of the first-run wizard's "Admin Tools" into Productivity (#159). **macOS 13.0+ required**, unchanged from v9.4.0. |
| [v9.4.6](v9.4.6.md) | 2026-08-09 | The integrated browser answers "how are users interacting with websites" without recording what was on the screen. `BrowserAnalytics` is the privacy boundary - a full URL goes in and only an eTLD+1 registrable domain comes out, with loopback, bare IPs and single-label intranet names reporting nothing - while `BrowserVisitTracker` separates dwell from active time, because a tab left open on a portal overnight would otherwise rank as the most-used site in the product. The in-page collector reads a fixed set of structural attributes in one function and nothing else: no `textContent`, value, placeholder, `aria-label`, id, `className`, `href` or clipboard, which is a reading restriction rather than a filtering one, so page content cannot leak through a bug in a later sanitizing step. Six review rounds hardened it: the sanitizers called themselves structural while `Char.isLowerCase()` accepted any script and `sanitizeFieldName` filtered with Unicode rules but redacted with ASCII ones, so an Arabic-digit record number passed both; the bridge was the first page-controlled producer on a 64-slot `tryEmit` bus with no rate cap, able to evict `AuthEvent` and `TabEvent` deliveries; `registrableDomain` stripped the scheme with `substringAfter("://")`, so a redirect URL carrying a scheme in its query reduced to the target domain, the exact smuggling its KDoc says cannot happen; and the kill switch only skipped injecting the collector while every `PAGE_VIEWED` and `TAB_*` event still reached the bus. `BOSS_BROWSER_TELEMETRY_DISABLED` now gates the single publish every browser event goes through (#98). Windows comes up as a plain browser on risalabs.ai with no panel opening by itself - a "Browser Only" workspace listed everywhere but default only there, a `settingsVersion` migration that reaches installs whose file already holds the old default, and one `StartupPanelPolicy` seam over the three effects that used to open Codebase and Run Configurations unasked. Focus mode switches each edge independently and keeps both sidebars on Windows, where the HARDWARE browser surface owns the pixels the reveal strips need, so hiding one would be a one-way door (#158). **macOS 13.0+ required**, unchanged from v9.4.0. |
| [v9.4.5](v9.4.5.md) | 2026-08-09 | Right-click opens a real macOS `NSMenu` instead of a Compose-drawn popup, which matters beyond styling: under `HARDWARE` rendering Chromium composites a native window over the Compose scene, so a drawn menu is painted behind the page it belongs to, while an OS menu is immune by construction rather than worked around. The engine lives in `plugin-ui-core` behind expect/actual so plugins get the same menus, and icons come from the macOS peer's `setImage` reached through the public `AWTAccessor` interface - with a real sizing bug behind it, since `CImage` treats a plain bitmap's pixel dimensions as its point size and rasterising at display density produced a 32pt icon on Retina. macOS only, on the same verified gate as BossTerm. Review found the fallback the whole design rests on was never taken, so a decline produced no menu at all, and that the tab menu's `DisposableEffect` keyed on a list rebuilt every recomposition, reopening an open menu under the cursor many times a second (#157). `plugin.json` `dependencies` were read in exactly one place - the uninstall check - so installing a plugin whose dependency was absent gave no signal at all and the user met it later as a feature that silently did nothing; the host now offers to install what is missing, from the three paths that install for a user and never from startup restore or a reload. The jar is vetted against the manifest before it loads, because nothing binds a store row to the id its jar declares and `installPlugin` starts an api hot swap for a newer api jar; it downloads to a `.jar.part` sibling, since `outputStream()` truncates on open and downloading onto the final name destroys any jar already there. Nine review rounds converged on one shared definition of installed - `LOADED || (jar exists && not recorded incompatible)` - after each single-clause version silently re-created the original silence for every later dependent (#156). A RISA Codex GLM gateway arrives for providers whose credential nobody types in, with the exchange kept host-side and brokers named by id rather than URL, so the worst a plugin can do is name a broker this build does not have (#136, #155). A plugin reload after an update was loading the jar the update had just deleted - the unload half succeeded and the load half failed, leaving fluck-browser gone for the rest of the session, and Reload All Plugins could do it to several at once (#151). And six dialogs plus every plugin toast were still rendering behind the browser: a scope bug rather than a missing call, since `LocalHeavyweightOverlays` covered a 20-line span while the dialogs composed later read its `false` default. The toast overlay needed a third, content-sized renderer, and its window size ratcheted shut invisibly because an empty host measures 32x32 from its own padding (#150). **macOS 13.0+ required**, unchanged from v9.4.0. |
| [v9.4.4](v9.4.4.md) | 2026-08-08 | Three resource tiers keep the process below the PartitionAlloc wall that killed 9.4.0: a Chromium renderer limit (4 at Lite, 2 at Ultra Lite), tier-aware hibernation timing published to plugins, and the background sampler off at the tightest tier. No tier takes a feature away - every plugin loads and every tab opens. Windows defaults to Lite and deliberately not Ultra Lite, because the renderer limit is a security control as much as a memory one; elsewhere the tier auto-selects by installed RAM with a watchdog reading real available memory per platform rather than the JDK's free-pages number, which reports 0.7% on a healthy 128 GB Mac. The organisation web pages move onto Blueprint with their contrast measured rather than eyeballed: the light theme rendered fine and had six sub-AA pairs in it, and four review rounds each found a surface the matrix did not list - the wash behind every role name and slug, the doubled wash a danger button is always read on, all six status borders, and `--line-strong`, the edge of every input and select, at 1.63:1. Cloudflare's Email Address Obfuscation was rewriting every member email into the literal words "email protected", blocked from decoding by our own nonce-only CSP, so one `<!--email_off-->` region now wraps each page. A signup landing between two deployments got two `user_roles` rows with an identical `assigned_at`, leaving the displayed `primary_role` plan-dependent for the life of the row; `clock_timestamp()` stops new ties and a repair migration nudges the ones already written. Also: an optional website on the organisation request, scheme-restricted in three places because any authenticated user supplies it and it renders as a link, and boss-organisation members finally carry a legible role. |
| [v9.4.3](v9.4.3.md) | 2026-08-07 | BOSS becomes multi-tenant. Organisations land as a first-class owner of secrets, plugins and membership: 9 tables, the permission catalog, RLS, 26 core RPCs, invite links, handoff tokens, JWT claims, a seeded `boss` org every existing user is backfilled into, and server-rendered member/admin/invite web pages as a new edge function. The existing `authorize()` is deliberately not the gate - it answers "does this user hold the permission anywhere" and short-circuits true for global admins, so every org-scoped decision reads the membership tables through `is_org_admin` / `is_org_member`. Two escalation paths shaped the rest: an org whose slug derived `boss_admin` would have handed its admins `role.create` and `role.assign`, and the `<slug>_user` hierarchy edge widens delegation, so a deny-list trigger makes `role.*`, `user.*`, `api_key.*`, `rpa.*`, `plugins.admin.*` and `organisation.approve|create` structurally un-grantable to an org role, failing closed and binding global admins and `service_role` too. Eight review rounds, mostly on things that were not the posture the migrations claimed: Supabase's `ALTER DEFAULT PRIVILEGES` had granted ALL on all nine org tables to `anon` and `authenticated` at CREATE TABLE time, so an org admin could SELECT `token_hash` from `organisation_invites` over PostgREST and 54 functions were anon-executable; `user_can_view_plugin_row` and `effective_share_role_ids` took arbitrary subjects over grants an earlier migration had revoked for that reason; the seeded boss-org roster was a global email directory, since every signup lands in it; a failed domain claim committed a half-created orphan organisation whose slug then blocked any retry, because a plain `RETURN` from PL/pgSQL rolls back nothing; reloading the invite-created page minted a second live invite; and the `?t=` handoff refused nothing cross-site, letting any page swap a victim's session for the attacker's identity. Store downloads are gated on `user_can_install_plugin` before the permission check and the download record, since a download URL is guessable from a plugin id and the listing gate was decoration (#138). The desktop half is the fallout: adding `secrets.org_id` emptied the secret panels on every installed build, because `SecretService` decoded with the strict kotlinx default and these RPCs return a *list*, so one unmodelled column throws and takes the whole page with it - and the real break was four RPCs, not the one reported, since a client decodes an RPC's `RETURNS TABLE` and those project derived fields existing in no table at all. One package-level `supabaseJson` now backs every decode site. Fixing it surfaced a credential leak: malformed input makes kotlinx append the whole offending document, which for these RPCs holds passwords the server has already decrypted, and the request direction leaked too, since a plugin calling `create_secret` puts the new password in caller-supplied parameters (#144). **macOS 13.0+ required**, unchanged from v9.4.0. |
| [v9.4.2](v9.4.2.md) | 2026-08-06 | A plugin crash no longer ends the session. Every exit from the crash dialog used to reach `System.exit(1)` - "Don't Send", Escape, even a *successful* report submission - while the title-bar close box disposed the frame and recovered nothing, so which control the user reached for changed the outcome; a crash attributable to a plugin now disables that plugin and keeps every window, tab and live terminal session, and only a fatal host crash still terminates. `PluginExecutionBoundary` tags the escaping throwable with the plugin id before the host calls in, since a registered callback leaves no plugin frame on the stack by the time the uncaught handler looks, and `PluginCrashRecovery` quarantines synchronously then unloads in the background. Nine review rounds, mostly on paths where the feature was worse than what it replaced: an `Error` escaping two `catch (e: Exception)` blocks held the dialog slot for good and silenced every later crash including a fatal one, suppression running before classification let a host `OutOfMemoryError` go to disk while the process carried on under heap exhaustion, and recovery quarantine was conflated with the ordinary render-fault marker so any plugin that had ever shown a fallback crashed silently thereafter (#139). Attribution decides who gets written out of `installed.json`, so it stops asking the classloader what its own id is - a nested loader could have named a rival - and moves to a host-installed resolver, first-install-wins, plus a review catch that the setter itself was reachable from plugin code and would have rerouted every attribution at once; it now happens at the invocation rather than at mapping time, which also stops handing Compose a fresh closure per menu item per recomposition (#142). Every remaining dialog and overlay routes above the `HARDWARE` browser surface - the "Open Link" dialog, all 25 Material alert dialogs, the Ctrl+Tab switcher and both drag ghosts were rendering behind the page, since Chromium's native child window composites above everything Compose paints - with the primitive moved into `plugin-ui-core` so plugins share one registry, a guard so a modal cannot act on the click that opened it, and anchored placement for popups corrected by the content-pane inset (#140). Configuration follow-ups from v9.4.1: a blank environment variable falls through in `ConfigLoader.resolve` at every tier rather than winning with `""`, which had made the dropdown, the command-line preview and the startup log all report a setting the engine did not use; settings edits compose through `updateAndGet` instead of overwriting from a stale snapshot; the settings file is created `rw-------` rather than chmod-ed after writing; and "Reset engine flags" is no longer a silent no-op after a corrupt load (#137, closes #134). **macOS 13.0+ required**, unchanged from v9.4.0. |
| [v9.4.1](v9.4.1.md) | 2026-08-05 | The embedded browser defaults to `HARDWARE_ACCELERATED` on macOS and Linux, joining Windows (#87). macOS moves for idle power and memory rather than throughput - it already scored 47.9 on Speedometer 3.1, ahead of Chrome, so a sweep there comes out flat and is the wrong instrument; the reading that matters is a content-matched idle A/B: CPU 0.59 → 0.06 cores (~10x), RSS 3095 → 1974 MB (-36%). Pinch-to-zoom would have broken silently, since under hardware the browser is not a Compose component and `Enter` never fires, so the gate is geometry-based now, with a px-to-dp conversion review caught being off by 2x on Retina. Every one of nine engine tunables becomes reachable from **Settings → Browser Engine**, fed through the existing `ConfigLoader` chain as system properties so an environment variable still wins and no read site had to change - capability-granting keys (sandbox opt-out, extra switches, DevTools port) deliberately bypass it so `local.properties` cannot enable them. Skia Graphite defaults on under hardware only, gated on the mode because the off-screen escape hatch must not inherit a backend observed blanking pages (#128). New default theme **Blueprint**, lifted line-by-line from bossconsole.ai so a user downloading from that page no longer lands in an amber UI, plus a `signalText` slot after review found ~90 sites drawing signal as text at 3.7:1 (#129). Out-of-process plugin children are reaped on host exit: the shutdown hook iterated a registry the plugin spawner never wrote to, so every exit including a clean Cmd+Q stranded the cohort - 434 orphaned JVMs holding 27 GB and 45k threads on one machine after 19 days; registration moved into `spawn()` itself, and the reap's cost no longer scales with the cohort (#131). The published download tables were fabricated - five 404 links per release across 9.4.0, 9.2.65, 9.2.64, 9.2.63 and 9.2.61, rendered identically by the in-app update dialog - now generated from each release's real asset list, filtered to what actually published, with pre-release links no longer resolving to an older stable (#127, #130). Also: the active-panel border reserves and paints its own ring, since Compose cannot clip a foreign native surface (#128); heavyweight overlays re-assert transparency after realization (#128); em-dashes are gone from prose with a CI guard that, unlike the first version, actually runs (#130); and the dead four-name LLM env probe is dropped from startup (#132). **macOS 13.0+ required**, unchanged from v9.4.0. |
| [v9.4.0](v9.4.0.md) | 2026-08-04 | Repair release for the JxBrowser 9.4.0 engine v9.3.0 shipped. The bump broke the browser for every existing install, and not because it was wrong: `main.kt` booted the engine twice - `prewarmInBackground()`, then `PasskeyPlatformInit.initialize()` - before `isChromiumInstalled()` ever ran, so both early boots threw `UnsatisfiedLinkError` against a now-stale on-disk engine before the download that repairs it was reached. Latent for as long as jar and engine matched. The guard moves above both boots, pre-warm is skipped when a download is needed, `CrossDeviceBrowserManager` stops booting Chromium in its constructor on the pre-UI path, and `FluckEngine` preflights so a mismatch names both versions instead of surfacing a path with no cause (#120). Guard and resolver were also checking different directories - `isChromiumInstalled()` inspects only `~/.boss/boss-chromium` while `getChromiumDir()` prefers the bundled engine - so a passing guard proved nothing about what boots and a stale *bundled* engine could never be repaired, the download writing to a cache the resolver ignored; the version check moved into the choice rather than a veto after it (#121/#122). Off macOS the bundled engine had no version check at all, `version.txt` being written only by the downloader: all five bundling sites in `release.yml` and three in `release-lite.yml` now stamp it, fail-open only where the stamp is absent, with a CI shell guard so a forgotten stamp is caught before publish (#123/#124). The engine download names the version it is fetching in all three phases, and a staged install offers a confirmed restart instead of a muted note that read like silent failure (#125). Deps: ktor 3.5.1, supabase 3.7.0, logback 1.6.1, grpc 1.83.1, junit-jupiter 6.1.2 - with the ktor/supabase coupling now recorded in `libs.versions.toml` (#126). **macOS 13.0+ required**, unchanged from v9.3.0. |
| [v9.3.0](v9.3.0.md) | 2026-08-04 | Minor-version release carrying the JxBrowser 9.4.0 / Chromium 151 engine. Selecting 9.4.0 in Settings previously bricked the browser: JxBrowser resolves its native toolkit under a directory named for the Chromium build its *Java* library was compiled against, so a jar pinned at 9.3.0 asked for `Versions/150.0.7871.47` while the installed engine shipped only `151.0.7922.72`, and `isChromiumInstalled()` compares `version.txt` against the Settings pin rather than against what the linked library needs - so it looked installed right up to the native load. The bundled library moves to 9.4.0 and becomes the default, and any pin that is not the bundled version is now cleared rather than honoured. **macOS 12 support ends with it** (9.4.0 dropped it; the engine bundle declares `LSMinimumSystemVersion 13.0`), which is only safe because six review rounds gated the delivery path on it: `generateMacOSUpdateScript` quits BOSS and `rm -rf`s the install with no OS check anywhere under `updater/`, so this release would have deleted a working macOS 12 install and left no way back. Both the Kotlin path and the shell guard now read the requirement from the *incoming* bundle before the quit, fail open on an unreadable plist or unknown current OS, and surface the real reason instead of a literal "Installation failed" (#99, follow-ups #118/#119). Every system plugin - api, Toolbox, the microkernel runtime, terminal-tab, editor-tab, fluck-browser - was sitting on disk unsigned, invisible only because enforcement defaults off; sidecars are now written on install and backfilled for the installed base, after review found the first cut never reached an up-to-date user and the second re-created the present-but-invalid state it exists to prevent by racing the update check and running before an access token existed (#101, flip tracked on #102). The crash dialog scrolls, so expanding "Technical Details" no longer pushes Report Issue / Don't Send / Clean Data & Restart off the bottom edge (#100), and its submit-failure card is sanitized with `sanitizeExceptionMessage` rather than `maskUriParams`, which was shipping the host and path verbatim in the string most likely to be pasted into a public issue (#107). Plus IPC `renameFile`, which could not overwrite on Windows and reported every failure on every platform as success (#103, fixes #96), and contact details moved off the defunct risa-labs.com domain (#117) |
| [v9.2.65](v9.2.65.md) | 2026-08-02 | Windows-weighted release. The browser defaults to `HARDWARE_ACCELERATED` there: `RenderingMode.OFF_SCREEN`, not Chromium switches, was the gap - fluck scored 18.1 on Speedometer 3.1 against Edge's 24.7 on the same Chromium 150, six switch sets were measured and none helped (four made it worse), and three other plausible causes were refuted by direct measurement; HARDWARE lands at 23.8, 96% of Edge, with macOS (47.9 off-screen, and HARDWARE costs the swipe-back gesture) and Linux (unmeasured) deliberately unchanged, the popup/modal/tooltip layering and surface-retention fixes ported from BossConsoleLite gated on `OverlayConfig.useHeavyweightPopups`, and `BOSS_RENDERING_MODE=OFF_SCREEN` as a no-rebuild escape hatch (#87). Two crashes that ended the app are now contained: a bookmarks-plugin layout bug that no host guard could attribute - by the layout phases the plugin's composables have returned, having emitted only `LayoutNode`s, so the stack holds no plugin frame and the host treated it as its own fault - now handled by `PluginRenderRecovery` attributing by which panels are mounted (rebuild, then narrow suspect-by-suspect, quarantining via `recordRenderFault` so no tabs close), with unattributed exceptions no longer fatal and five post-merge defects fixed including a containment budget that raced the narrowing loop and lost, un-reference-counted mount tracking that ruled the real culprit out by omission, and a panel fault closing that plugin's unrelated main tab (#88, #90); and a right-click in an OAuth popup, where JxBrowser's own `SuggestionsPopup` resolves its position from `getLocationOnScreen()` inside an `invokeLater` and a popup that closes itself mid-flow is exactly the window where that hits a disposed component - replaced with a Cut/Copy/Paste/Select All menu whose enablement derives purely from the click target, paired structurally with the page-click dismiss handler (#89). Favicons update on Windows again: `File.renameTo` fails with `ERROR_ALREADY_EXISTS` where POSIX `rename(2)` replaces, so a URL's icon could be written exactly once ever - and the failure returned null, which *reset* the tab to the default globe rather than merely leaving it stale (#95). The MCP per-tool kill-switch now fails closed on both I/O paths - it is the only control that survives `permitted()`'s `isAdmin` short-circuit, yet a corrupt disabled-tools file silently re-enabled everything and a failed write applied the change in memory and lost it at the next launch (#97, closes #85). Plus a least-privilege `boss_plugin_admin` role with `plugins.create` enforced on all five publish routes, which previously checked only that you were signed in (#93), a scheduled JxBrowser engine auto-release watcher (#94), a Speedometer 3.1 cross-browser report with its harness (#83), and a README correction scoping the infra plugins' agent guardrails to what the code actually enforces (#84) |
| [v9.2.64](v9.2.64.md) | 2026-07-31 | Three-fix release across the browser, plugin loading and the release pipeline. The browser context menu now reads its target from `ShowContextMenuCallback.Params` instead of injected page state - link href stashed on `window`, `window.getSelection()` and `document.activeElement` all answer for the main frame only, so a click inside an iframe reported the *previous* click's link and any click after focusing a search box reported an editable field, while images were never reported despite `hasImage`/`imageUrl` being in the API all along; the request is now answered unconditionally from a `finally` with no live-`Browser` call on the path (an unanswered one showed no menu at all), the four blocking `executeJavaScript` round-trips before `tell.close()` are gone with 140 lines of dead injected JS, and editable stays main-frame-only on purpose since every action it unlocks targets `mainFrame()` (#78). Plugins that merely hold a `ComponentLogger`, `Bookmark` or `TabConfig` as a *property* can load again: the host-bundled copies of eight `boss-plugin-api` packages shadow the api's parent-first but were compiled without Compose, so a plugin's `getstatic` for the synthetic `$stable` field linked at build time and vanished at load time - `BinaryCompatibilityValidator` then rejected the *entire* plugin (secret-manager 1.2.6/1.2.7 were unloadable on every host over one field). Diffing every duplicated package found it missing from **15 classes** across `plugin-logging`, `plugin-bookmark-types` and `plugin-workspace-types`, all now emitting it - repairing already-built plugins with no api release - plus a `$stable`-specific validator diagnostic, three `*StableFieldTest`s and publish tasks that depend on their own `desktopTest`; the three modules go 1.0.4 → 1.0.5 (#80). And the Windows ARM64 build is restored after the 30 Jul release published without that artifact - the leg failed compiling `composeApp` while the other four platforms and `create-release` succeeded, so a partial release looked green: `SelfHealingSettings.kt` was the one file under `kernel/` that needed no `boss-ipc` yet was excluded by package name, and the non-excluded settings UI imported three types from it. It moves to `ai.rever.boss.config` with its tests, and a new `WindowsArm64SourceIsolationTest` that runs everywhere pins the isolation rules against `build.gradle.kts` - immediately finding a second live break in two `boss-ipc`-importing tests (#82) |
| [v9.2.63](v9.2.63.md) | 2026-07-30 | Single-change release: the host stops owning AI provider configuration. **Settings → LLM Providers** becomes **Settings → AI Providers**, rendered by the secret-manager plugin that now holds the credentials, the environment-variable resolution and a live model catalogue - keys stored as encrypted secrets instead of plaintext on disk, model lists fetched from the providers themselves. The host's ~1,400-line LLM stack is deleted: `LLMModelFetcher` (a "dynamic" fetcher that returned hardcoded, years-stale lists with no-op caching), `LLMSettings` and its 40-entry `LLMModel` enum whose constructor took a provider and discarded it, `DesktopLLMSettingsManager`, and the startup effect that loaded it. `LlmProviderAPIAccess` mirrors `EditorAPIAccess` as the single relay, backing both the settings section and `PluginContext.llmProvider` - which the host had never implemented, so plugins offering AI features got `null`; it resolves against *this* window's registry rather than the per-window-overwritten singleton. Self-healing keeps working across the migration: it resolves a key before any plugin registers, so it reads the legacy key file directly and now also tries `llm_settings.json.migrated` (the name the plugin's one-shot import renames the original to), extracted as a testable `legacyKeyFrom` with nine cases; the three UI strings pointing operators at LLM Providers now name `AI_REPAIR_API_KEY` or the provider's own environment variable. Pins `boss-plugin-api` 1.0.71 (#77) |
| [v9.2.61](v9.2.61.md) | 2026-07-28 | Security-and-correctness release across the external surfaces. The passkey edge function now verifies what it signs - the challenge inside `clientDataJSON` (with the storage lookup keyed on it), `authData.rpIdHash` pinned per credential, a monotonic signature counter under compare-and-set, ES256 **and** RS256, either base64 alphabet - plus unconditional single-use challenge consumption, an authenticated `/register/challenge` so a caller can't mint a challenge for another account, authenticated `/manage/*`, and `create_mobile_registration_session` revoked from `PUBLIC` (#46, #56). `boss://` links now carry a `DeepLinkOrigin` so only BOSS's own CLI runs a `terminal?command=` unprompted, the single-instance lock moves from the shared system temp directory to a token-authenticated owner-only descriptor in `~/.boss/run` over a Unix-domain socket, and log sanitizing masks credentials that reach a message with no URL around them (#60). A closing plugin classloader no longer resolves the plugin's own classes against the host - the `LinkageError` that crashed terminal-tab hot reload - and the accidental ktor **server** stack (529 classes) is off the host classpath with a CI guard (#57). New: **Help ▸ Import Passwords & Bookmarks…** reads Chromium/Safari/Firefox profiles natively or their CSV/HTML exports into the encrypted vault, never decrypting during the scan (#67); and plugin UI over IPC works in both directions for the first time via `PluginUIServiceBridge` + `RemoteUiSurfaceRegistry`, with key, scroll and lifecycle emitters and eight renderer-contract defects fixed (#48, #50, #51). Also: browser history only records pages that actually loaded, with the destructive path fenced against DNS outages (#66), suggestion ranking is no longer recency wearing a visit-count label (#70), the updater gets one owner so closing a window can't kill every window's checks (#47), deep links resolve their target window once at dispatch (#44), the macOS floor becomes a real 12.0 and the `.deb` installs headless (#45, #49), orchestrator manifest reads are confined and approvals reported honestly (#68), and a dropped notarization poll resumes instead of losing the release (#41). |
| [v9.2.60](v9.2.60.md) | 2026-07-25 | Fixes browser video fullscreen requested from an already-fullscreen window - a dedicated `FullscreenBrowserWindow` overlay path is chosen from real window/screen geometry, transitions are scoped to the owning browser and guarded by a lifecycle epoch, and overlay teardown is guaranteed across early native exits and exit/re-entry races (#36). A new thread-safe `WindowFocusManager` gates browser key routing on the owner window's focus so typing can't leak between windows (#28, the unpublished v9.2.59 change), and the tab bar's "+" button returns to a trailing slot beside the last tab in Shrink to Fit mode, with the shrink math reserving that slot and rounding the divider reserve per rendered piece to stop scrollbar flicker at 150%/175% scaling (#40). Also the quality-gate wave: the tree is formatted to `ktlint_official` with the gate on, detekt gates all modules against frozen baselines, the `BossDark*` → semantic-token migration is finished, dead platform source sets and the `shared/` template module are deleted, and the compiler runs warning-free (#20, #21, #22, #23, #24, #25, #32, #33). |
| [v9.2.58](v9.2.58.md) | 2026-07-24 | Fixes multi-window tab cleanup so closing one window no longer tears down another window's plugin tabs. Each window's `DynamicPluginManager` now disposes only its own tabs via a new `disposeWindow()` / `closeTabsAcrossWindows` flag, while the global teardown callback (explicit update/reload/uninstall) still spans every window. A new `BrowserWindowOwnershipRegistry` tracks which window owns each plugin browser handle and atomically marks a window closed before draining its handles - a creation that finishes mid-teardown is rejected and disposed synchronously instead of retaining its destroyed AWT window - and `BrowserServiceImpl.createBrowser` now rejects unscoped calls in favor of the window-scoped `createBrowserForWindow` path (#27). |
| [v9.2.57](v9.2.57.md) | 2026-07-23 | Brings Safari-style tab shrinking to the top tab bar - every tab scales uniformly to fit the row width and only clamps to a 36dp favicon floor (and scrolls) once tabs would drop below it - with a new **Settings → Window** Tab Sizing toggle between Shrink to Fit (default) and Fixed Width. Per-tab width is measured via `onSizeChanged` on the `LazyRow` rather than a nested `BoxWithConstraints` (avoiding the double-`SubcomposeLayout` that pinned the EDT at 100% CPU past ~10 tabs) and computed in integer pixels so total content stays ≤ row width and the scrollbar stops flickering; the "+" button becomes a permanent sibling outside the row so it no longer scrolls off-screen (#26). |
| [v9.2.56](v9.2.56.md) | 2026-07-23 | Routes ~380 hardcoded color literals across ~35 UI files through the Operator's Console design system so every component honors live theme switching, decomposes the monolithic `BossApp.kt` into focused effect files (with breadcrumbs on seven previously silent catches), extracts a shared editor syntax palette, and lands contrast/accessibility fixes plus a stale-Split-menu fix (#18). |
| [v9.2.55](v9.2.55.md) | 2026-07-23 | Fixes an in-app updater failure on macOS App Translocation so updates restart the app correctly (#16), and closes a public-CI hole where DigiCert signing secrets could leak into build logs (#6). Also bumps the `boss-plugin-api` pin to 1.0.68 (#17), lands a CI change to commit automated version bumps under a human identity rather than a bot (#9), and ships a large documentation refresh - a showcase rewrite of the README (AI agents, governance, Toolbox, BossTerm sharing), a new "How BOSS compares" open-source capability matrix, Toolbox/MCP docs, and agent guidance shared across Codex and Claude (#7, #8, #10, #11, #12, #13, #14, #15). |
| [v9.2.54](v9.2.54.md) | 2026-07-21 | Maintenance release - no functional code changes since v9.2.53; the version was bumped to republish fresh, signed multi-platform builds. |
| [v9.2.52](v9.2.52.md) | 2026-07-20 | Opens `.ipynb` files in a dedicated Jupyter notebook tab and unbreaks the Release Build. `.ipynb` files opened from the file tree, bookmarks, or **New Tab → Jupyter** now route to a `JupyterTabInfo` tab (typeId `jupyter`, added to plugin-api-core) rendered by the jupyter-notebook plugin - with an optional notebook-name field flowing to the tab title, workspace save/restore, split copies getting a fresh id, an applied `updateTitle`, and a graceful fall back to the code editor when the plugin isn't installed; also adds a platform-aware **Reveal in Finder** tab right-click action via a shared `commonMain revealInFileManager` util, and skips macOS signing when the Developer ID cert is absent so local builds go unsigned instead of failing (#854). Fixes the 🚀 Release Build failing under the configuration cache on two platforms: `signPty4jBinaries`/`extractCLIToAppResources` captured non-serializable script-level values in their `onlyIf`/`doLast` lambdas (now captured task-local at configuration time), and Windows-ARM64 used a type-safe accessor for the `plugin-api-ipc` module `settings.gradle.kts` excludes there (now resolved by path behind a `findProject` guard, with `SkipListDriftTest.kt` excluded when the module is absent) (#888) |
| [v9.2.50](v9.2.50.md) | 2026-07-20 | Threads a `showHidden` opt-in through the read-side file scanners so plugins can list dot-entries on demand - `FileSystemDataProvider` advertises `supportsHiddenEntries = true` and the flag flows through `scanDirectory` / `scanDirectoryWithDepth` / `directoryHasChildren`, works end-to-end for out-of-process plugins via the IPC proxy (the `FileSystemService` proto has carried `include_hidden` since v1), and shares the visibility filter from `commonMain` so desktop/android can't drift; legacy methods stay dot-filtered and `build`/`node_modules` stay skipped, letting the Codebase plugin retire its duplicated scanner (requires boss-plugin-api 1.0.66) (#881). Also tidies the repo layout with two purely-cosmetic moves: the 13 `boss-*` microkernel modules relocate under `modules/` and `plugins/` is renamed to `plugin-platform/` (host-side plugin SDK), both preserving flat Gradle paths and type-safe accessors so no build files or behavior change (#879, #880) |
| [v9.2.49](v9.2.49.md) | 2026-07-20 | Fixes the release-publishing pipeline that made v9.2.48 invisible to the in-app updater. Resumable uploads sliced chunks by index (`dd bs=CHUNK_SIZE skip=offset/CHUNK_SIZE`), so a partial-PATCH resume on a non-`CHUNK_SIZE`-boundary offset aborted with `misaligned offset` instead of resuming - a transient large-asset blip became a hard publish failure that skipped the `app_releases` upsert, leaving the release on GitHub + in the bucket but absent from the table `latest-release` reads. `slice_chunk()` now slices byte-exactly via portable `tail\|head` (any resume offset valid), and a post-publish assertion reads the row back and fails loudly if it's missing. Also hardened SIGPIPE-safe/testable with a `lib/tus-slice.sh` extraction + `test-tus-slice.sh` wired into CI as a 🐚 Shell Script Tests job (#877, #878) |
| [v9.2.48](v9.2.48.md) | 2026-07-19 | Removes the never-used plugin dependency resolver (~590 net lines) and hardens `SemanticVersion` - now the sole version-comparison primitive gating plugin updates. BOSS expresses cross-plugin needs via soft runtime lookup (`context.getPluginAPI`), not the manifest `dependencies` field, so `PluginDependencyResolver` (hard, version-ranged, resolve-before-load) had zero input and zero callers; it, its result types, `VersionRange`/`VersionConstraint`, the zero-thrower `PluginDependencyException`, and composeApp's dead module edge are removed. `SemanticVersion` now implements SemVer §11 prerelease precedence identifier-wise (fixing `rc.10` sorting below `rc.9`, which flows into `PluginUpdateManager.isNewerVersion`), drops the `build` field that broke the `Comparable` contract, rejects malformed inputs (`1..2`, `1.x`, negatives, empty sections), and adds 12 unit tests - with its now-dead deps trimmed since it is pure Kotlin (#876) |
| [v9.2.47](v9.2.47.md) | 2026-07-19 | Signs store plugins and verifies those signatures at plugin load time - the choke point every install path funnels through - so a rewritten store row or storage object can no longer smuggle a different JAR onto a user's machine. `#871` wired verification into `RemotePluginRepository`, but the Toolbox uses its own `downloadFromStore` and never hit that path, so store installs/updates (the dominant surface) were checksum-only; verification now lives in `DynamicPluginLoader.loadPlugin`, checking a `<jar>.sig` sidecar over the anchor `pluginId\|version\|sha256` against the pinned key (present-but-invalid rejects; absent warns, dev side-loads exempt via `boss.dev.mode`, hard-fail once enforcement is enabled). The store publish function signs each version's server-computed sha256 (RSASSA-PKCS1-v1_5/SHA-256) with a Supabase-secret key, binds signature → hash → bytes via a strict `verifyDigestSignature` path, asserts the JAR manifest matches the signed row, and ships a `plugin_versions.signature` migration plus a DB-only backfill (#875, #871). Also removes the orphaned RPA panel subtree (live browser-integration pieces extracted verbatim) and ~2,850 more lines of dead code with zero references across four repos, with no behavior change (#870, #869) |
| [v9.2.46](v9.2.46.md) | 2026-07-16 | Gives the browser home page a stable identity so back-navigating to it shows a Home icon and "Home" title instead of a blank tab with a stale favicon. `FluckTabInfo.isHomeUrl`/`HOME_TITLE` becomes the single definition of the home state (used by both the tab icon logic and `BossTabUpdateProvider.updateUrl`, previously duplicated), a home landing records "Home" in navigation history and clears the favicon cache key, and `updateTitle` ignores blank titles - with the fix holding for older fluck-browser plugin versions and covered by a new `FluckTabInfoTest`. Also refreshes four backend dependencies: kotlinx-serialization 1.9.0→1.11.0, gradle-wrapper 9.5.1→9.6.1, logback-classic, protoc-gen-grpc-java, and grpc 1.82.1→1.82.2 (#865) |
| [v9.2.45](v9.2.45.md) | 2026-07-12 | Audits the JxBrowser engine flags for Chromium 150, pre-warms the engine on a background thread, and makes Widevine activation async. The per-platform, OFF_SCREEN-aware flag audit drops now-default-on/blocklist-forcing GPU flags, restores the sandbox (`BOSS_CHROMIUM_DISABLE_SANDBOX` escape hatch), scopes `--disable-dev-shm-usage` to containers, disables Windows `CalculateNativeWinOcclusion`, enables Linux VA-API decode, sizes a 512MB disk cache, and exposes `BOSS_CHROMIUM_EXTRA_SWITCHES` for no-rebuild A/B testing - with the decision extracted as a pure `performanceSwitchesFor(...)` under unit test. `FluckEngine.prewarmInBackground()` boots the engine off the UI thread from `main.kt` instead of synchronously inside the first browser tab's composition (a multi-second cold-start freeze), gated on the browser profile already existing (opt out via `BOSS_BROWSER_PREWARM=false`) and self-healing a background-boot failure so it can never make `isAvailable()` report false, with `@Volatile` init-state fields guarding the UI-thread/pre-warm-thread interleavings. Widevine CDM activation is no longer `join()`ed during engine creation, and `isAvailable()` no longer triggers engine init from the UI thread. SkiaGraphite was A/B-tested live on Apple Silicon and reverted to opt-in (`BOSS_ENABLE_SKIA_GRAPHITE=true`) because it blanks the OSR frame output on JxBrowser 9.3.0 (#864) |
| [v9.2.44](v9.2.44.md) | 2026-07-12 | Adds a **System Default** option to the terminal link-open dialog. `TerminalLinkOpenMode` gains `SYSTEM_DEFAULT` - a new option card in `TerminalLinkOpenDialog` and an entry in the Terminal Links settings dropdown - so URLs and files clicked in a terminal can be routed to the OS default handler as an explicit, rememberable choice: URLs open via the existing `openUrlInBrowser` expect/actual and files via a new `openFileWithSystemDefault` backed by `FileSystemUtils.openFile` (Finder/Preview, not just a browser), with a mode-agnostic `AutoMirrored.Outlined.Launch` icon. Pairs with BossTerm #334, which forwards CLI-originated `open`/`xdg-open`/`$BROWSER` requests into the same link-click pipeline so those now show the dialog instead of silently opening the OS default. Adds `TerminalLinkSettingsSerializationTest` guarding that every mode round-trips through persisted JSON against enum renames/reordering, plus log-and-noop actuals on the disabled android/ios/wasmJs targets (#863) |
| [v9.2.43](v9.2.43.md) | 2026-07-12 | Fixes crash reporting for end users, which failed with "GitHub authentication required" because the client created GitHub issues directly and needed a token no user machine has. A new server-side crash-report Edge Function now holds `GITHUB_TOKEN` and files each issue in the crashing plugin's own repo (`system_plugins.github_repo`, then `plugins.homepage_url`) - or `BossConsole-Releases` for host crashes - with signature dedup and a fallback to the default repo if the plugin repo rejects it; `CrashHandler` attributes each crash to a plugin via the first `PluginClassLoader`-defined stack frame (the loader keeps a weak process-wide instance registry), and `CrashReportService` submits through the proxy first with direct GitHub kept only as a dev-machine fallback. Dedup scans the 100 newest open crash-report issues via the issues list API rather than `/search/issues` (the search index lags seconds-to-minutes, so a crash loop would otherwise file a duplicate per crash). The function is hardened with a `[functions.crash-report]` config.toml entry (`verify_jwt=false`), an anon-key header gate against anonymous issue creation, a per-IP 20/hour rate limit consumed only after validation, and CORS dropped entirely (#862). Also makes the dashboard header responsive to narrow windows - the stats card no longer overlaps the welcome title and stat labels no longer wrap letter-by-letter: under 700dp the stats card stacks below the title via `BoxWithConstraints` and at very narrow widths it scrolls horizontally instead of overflowing (#861) |
| [v9.2.42](v9.2.42.md) | 2026-07-11 | Caps the number of plugin icons shown per sidebar slot behind a vertical three-dot "More" overflow menu so a crowded rail never clips - each icon slot shows a limited number of plugin icons and collapses the rest into a "More" menu that toggles panels exactly like the icons, with a new **Settings → Sidebar** cap that is either ADAPTIVE (default: each bar splits its measured height into a 40dp icon-row budget round-robin across slots so no slot starves its neighbours, an overflowing slot spends its last row on the More button, and every plugin stays reachable at any window size) or FIXED (1..20 icons per slot, persisted in `sidebar-visibility-settings.json`), plus a right-click "Sidebar settings" entry on every icon and the More button (#860). Fixes trackpad pinch zooming the fluck browser even when the browser tab was hidden or in a different split: the macOS magnification listener was attached window-wide, fired unconditionally, and leaked a new permanent listener on every browser-view mount - pinch now only zooms when the cursor is over that `BrowserView` (registered via a `DisposableEffect` removed on hide/close/dispose, bound to the actual hosting window via `LocalWindowId`), same-domain tabs zoom independently via `ZoomMode.PER_BROWSER`, and the gesture-listener proxy gets identity `equals`/`hashCode` so removal no longer silently leaks (#857). Also refreshes open sidebar panels on plugin hot reload - an open panel kept its cached component, so a reload that swapped the registry factories (unload+load, Toolbox update, disable→enable) kept rendering the old build and pinned the pre-reload classloader; a new `PanelComponentStoreRegistry` + `DynamicPluginManager.pluginPanelsRefresh` hook now reset every matching open slot in every window on the EDT once the new build registers (#858, fixes #856) |
| [v9.2.41](v9.2.41.md) | 2026-07-11 | Recovers from Supabase session-refresh failures instead of crash-looping. A session stuck in `RefreshFailure` was previously unhandled by `CoreAuthService` (only `Authenticated`/`NotAuthenticated` were), so after a failed token refresh the app sat on an expired session while supabase-kt's Realtime reconnect loop threw `TokenExpiredException` into library-internal coroutines every ~40s - each hitting `CrashHandler` as an uncaught exception and popping the crash dialog (whose dismissal exits the app). `CoreAuthService` now handles `SessionStatus.RefreshFailure` and runs a single-flight, exponential-backoff (10s→60s) recovery loop; a testable `SessionRecoveryPolicy` decides retry-vs-clear (transient network/5xx/Cloudflare/408/429 retry without signing out; remaining 4xx rejections of the refresh token clear the session via `clearUnrecoverableSession()` and drop the user on the login screen instead of a zombie state). A first-status-after-startup `RefreshFailure` resolves the session and surfaces **Offline** rather than hanging on the Loading spinner; `signOut()` `cancelAndJoin()`s any in-flight recovery (`recoveryJob` is `@Volatile`) so it can't race sign-out; and `CrashHandler` now classifies `TokenExpiredException` (incl. cause chains) as ignorable since it is a recoverable background auth failure, not an app crash (#855) |
| [v9.2.40](v9.2.40.md) | 2026-07-10 | Fixes moving a live tab between split panels. `MoveToPanel` and cross-panel `CreateSplit` drops now detach the live component from the source panel and let the target panel adopt it as-is, instead of destroying and recreating it from config - so browser tabs no longer reload from their URL and, critically, the old browser instance no longer keeps running invisibly (YouTube audio kept playing from the leaked Chromium process). Each tab component also gets its own destroyable `LifecycleRegistry` (resumed on add, destroyed on remove) rather than sharing its panel's never-destroyed one, so a dynamic plugin tab's `lifecycle.onDestroy` cleanup (e.g. fluck-browser disposing its JxBrowser handle) fires and closing *or* moving a browser tab no longer leaks a live process. `TabUpdateRegistry.createProvider` now re-resolves the tab's owning panel on every call so a cached provider keeps updating the tab bar after a move; same-panel edge drops and context-menu Split Right/Down keep their existing copy semantics (#853) |
| [v9.2.39](v9.2.39.md) | 2026-07-10 | Upgrades JxBrowser to 9.3.0 (Chromium 150) with a renewed license. The new engine ships a native macOS Touch ID / WebAuthn platform authenticator inside branded Chromium, so BOSS deletes its ~1,337-line custom in-process passkey implementation (#823) - which was inert anyway since its Apple entitlement was never granted - and passkeys **improve** (native Touch ID works out of the box). The branding pipeline now preserves the Developer ID identity + entitlements on re-sign, commits the Developer ID provisioning profile, and adds a keychain entitlement so Touch ID-verified passkeys created in the BOSS browser work; supporting fixes cover a TCC `SIGABRT` from Chromium 150's Bluetooth-probing WebAuthn hybrid transport (add `NSBluetooth*UsageDescription`), the renamed `com.teamdev.Platinum` singleton lock, flattened framework symlinks in the macOS engine zips (broke 9.3.0's JNI load with `UnsatisfiedLinkError`), and Supabase re-publish `409`s. Separately, hardens startup plugin loading so a background system-plugin update (v1.8.7 Toolbox incident) that rewrites `installed.json` and swaps the JAR mid-startup no longer leaves the plugin unloaded for the whole session - the persisted pass re-resolves relocated jars by `pluginId` and the external scan lists the directory after it (#852, #850) |
| [v9.2.38](v9.2.38.md) | 2026-07-09 | Fixes plugin self-reloads. `PluginLoaderDelegate.reloadPlugin` previously ran its unload+load on the *caller's* coroutine, so a plugin reloading its own plugin (Toolbox self-update, evolver hot-reload) force-unloaded itself and cancelled the scope between unload and load - leaving the plugin unloaded and never reloaded (why Toolbox self-updates still needed a restart). Mirroring the #848 `hotSwapApiLayer` pattern, the orchestration now runs as a detached job on a delegate-owned scope (never cancelled; in-flight reloads must outlive their initiator), so a caller torn down by its own reload gets the cancellation at `await` while the job completes, with an `invokeOnCompletion` handler logging any unobserved throw. A new `KeyedDetachedJobs` coalesces concurrent `reloadPlugin` calls for one `pluginId` into the in-flight job (identity-checked removal so a finished job can't evict a newer one) rather than interleaving two uninstall+load sequences, and a `NoClassDefFoundError` from a closed classloader reaches the `onDetachedFailure` hook instead of vanishing with a torn-down caller. `resetPluginInstances` inherits the protection and now rethrows `CancellationException` before its general catch, so the self-reset case no longer surfaces a spurious "Exception resetting plugin instances" error and a wrong `0` return - unlocking restart-free Toolbox self-updates (#849) |
| [v9.2.37](v9.2.37.md) | 2026-07-09 | Fixes the plugin-platform API hot swap. Force-unload now reaches the loader: `uninstallPlugin(force = true)` flows through to `unloadPlugin` (which previously re-checked `canUnload` unconditionally), so the four `canUnload=false` system plugins (plugin-manager, terminal-tab, editor-tab, fluck-browser) no longer survive the unload parented to the closed old `ApiClassLoader` and fail reload as "already loaded" - a live 1.0.63→1.0.64 swap now cycles all 29 plugins in ~1.5s instead of stranding 4. Also tears down plugin UI before the API-layer swap closes loaders: `hotSwapApiLayer` invokes `PluginLoaderDelegateImpl.teardownAllPluginTabs` to close every plugin tab across all windows on the EDT before the unload loop, so Compose `onDispose` (e.g. BossTerm's terminal disposal) resolves against still-open loaders instead of raising `NoClassDefFoundError` and popping `CrashHandler` dialogs (#848) |
| [v9.2.36](v9.2.36.md) | 2026-07-08 | Turns the plugin system into a full platform. Adds a hot-swappable, runtime-updatable API layer - the `boss-plugin-api` surface no longer compiles into the host (plugin-api-core drops ~9,300 lines of API source); classes load at runtime through a process-wide `ApiClassLoader`, with `minApiVersion` gates rejecting plugins built against a newer API and store-only API distribution (local api-package filtering kept just for compilation). Adds five plugin-extensible host UI registries - status bar, panel ⋮ menu, keyboard shortcuts, settings pages, and deep-link actions (`StatusBarRegistryImpl`, `PanelMenuRegistryImpl`, `PluginShortcutRegistryImpl`, `SettingsPageRegistryImpl`, `DeepLinkActionRegistryImpl`) - each rendering inside a `PluginExtensionBoundary` crash boundary with RBAC/ownership gating (`AccessGatedRegistry`, `PluginOwner`) so a misbehaving plugin can't take down the host UI. Adds a Supabase-backed system-plugins manifest (`SystemPluginManifestService`, migration `20260708000000_system_plugins_manifest.sql`) resolving the system plugin set remotely instead of hard-coding it (#847). Fixes the Toolbox setup wizard re-popping on every restart: the wizard check raced asynchronous startup plugin loading and, on winning, saw an empty registry and re-showed the wizard despite completion - it now awaits `DefaultPlugin.awaitInitialPluginLoad()` (joining the persisted + external-scan jobs, 30s cap) before trusting the installed-plugin count (#846) |
| [v9.2.35](v9.2.35.md) | 2026-07-08 | Host integration for the Tool Evolver plugin (formerly Tool Sidecar). New sidebar panel ⋮ menu items - "Open Evolver" (gated on the permission-gated `evolver_evolve` tool) and "Report Issue" (gated on the ungated `evolver_open`) - appear only while the plugin is active and dispatch through the RBAC-filtered `McpToolRegistry` (via `McpToolRegistryImpl.invoke`) with no compile-time coupling; failures surface a UI warning and `StatusMessageManager` message. Adds split-open APIs `SplitViewOperations.openTabInSplit`/`openUrlInSplit` (mirrored into plugin-api-core, implemented in `SplitViewOperationsImpl`) backing the Evolver's new-tab-vs-split and issue-link choosers, and mirrors `ConsoleLogsAPI` + `PluginLogMatcher` (api 1.0.59) into the host. Fixes: `TrackingPluginContext` and `SandboxedPluginContext` now forward `directoryPickerProvider`/`projectDataProvider` (both fell through to `null`, so "Open Project" and the Evolve "Browse" button silently did nothing); the directory dialog is owned by the active window with `alwaysOnTop` so it no longer opens behind the app on macOS; and `CrashHandler.isIgnorable()` logs+swallows closed-channel/broken-pipe/connection-reset/cancellation exceptions so a plugin hot-swap dropping the terminal-tab MCP writer socket no longer pops the "BOSS Has Crashed" dialog (#838) |
| [v9.2.34](v9.2.34.md) | 2026-07-06 | Adds `PanelEventProvider.openPanel(panelId, windowId)` so a plugin can reveal another plugin's sidebar panel - the same reveal path `boss://plugin?id=` uses. A new default-no-op method on plugin-api-core's `PanelEventProvider` is implemented in `PanelEventProviderImpl` via `PanelEventBus.openPanel` (used by the Toolbox "Create" tab to launch Tool Creator), and forwarded over gRPC (new `OpenPanel` RPC + `OpenPanelRequest` in `panel_event.proto`, a `PanelEventProviderProxy` override, and `PanelEventServiceBridge` handling) so out-of-process plugins work too - without it a kernel-mode plugin's `openPanel` fell through to the interface default no-op and silently did nothing. Pairs with boss-plugin-api 1.0.57 (#844) |
| [v9.2.33](v9.2.33.md) | 2026-07-06 | Maintenance release with no functional code changes - a version bump on top of v9.2.32 to publish a fresh signed multi-platform build. The only in-range commit is documentation (the v9.2.32 release notes); all v9.2.32 functionality carries forward unchanged, including the macOS launch fix that removed the still-ungranted `com.apple.developer.web-browser.public-key-credential` managed-capability entitlement from `BOSS.entitlements` (#836) |
| [v9.2.32](v9.2.32.md) | 2026-07-05 | macOS launch fix. Removes the still-ungranted `com.apple.developer.web-browser.public-key-credential` managed-capability entitlement from `BOSS.entitlements` - macOS refuses to open a Developer ID app that declares it without a matching embedded provisioning profile, and Apple's approval (request `YWD6D3446V`) is still pending, so every macOS launch failed with "The application can't be opened" once #835's XML-comment fix stopped masking the earlier `codesign` failure. The `BossWebAuthn.swift` provisioning guard already treats native platform passkeys as unavailable until the profile is embedded, so removing the key is runtime-neutral (#836) |
| [v9.2.31](v9.2.31.md) | 2026-07-04 | Feature release. Adds native macOS platform passkeys (iCloud Keychain) for in-browser WebAuthn - JxBrowser's embedded Chromium exposes no platform authenticator, so BOSS routes `navigator.credentials.get/create` to Apple's `ASAuthorizationWebBrowserPublicKeyCredentialManager` (universal in-process `libBossWebAuthn.dylib`) behind origin binding to the engine's real top-frame URL, real PSL-based `rpId` validation, top-frame/popup-aware injection, ES256-only routing with USB fallback, `excludeCredentials`→`InvalidStateError` mapping, and denial recovery to Chromium; a runtime provisioning probe keeps the whole feature a strict no-op until Apple provisions the App ID (#823). Browser-engine archives now resolve Supabase `app_releases` (`boss-chromium`) first with GitHub backup and sha256 verification, plus a new Browser Engine settings section to view/pin versions with staged installs swapped in safely at startup (#832). Adds a `boss://split?orientation=vertical\|horizontal` MCP deep link to split the main window with reliable window resolution (#834). Fixes an invalid `--` inside a `BOSS.entitlements` XML comment that broke every macOS `codesign` step with an AMFI parse error (#835), the setup download window clipping Retry/Exit in its error state (#832), and the update dialog's "What's new" now showing curated `docs/release-notes` notes instead of the auto-generated boilerplate (#831); also relaxes the CLAUDE.md `./gradlew run` rule to allow a bottom split pane and documents the terminal MCP servers (#833) |
| [v9.2.28](v9.2.28.md) | 2026-07-03 | Maintenance release with no functional code changes - a version bump on top of v9.2.27 to publish a fresh signed multi-platform build. The only in-range commit is documentation (the v9.2.27 release notes); all v9.2.27 functionality (Toolbox 1.8.4 stability work - plugin updates gated on `minBossVersion`, `McpServerController.setPort` in the host interface, sequenced startup plugin passes, and best-effort `.deb` desktop-menu integration for headless installs) carries forward unchanged |
| [v9.2.27](v9.2.27.md) | 2026-07-03 | Stability release triggered by the Toolbox 1.8.4 incident. The plugin updater now gates store candidates on `minBossVersion` before replacing the working jar (mirroring the IPC gate, reported as `IncompatibleNotice`, failing open on blank/unparseable versions), maps `minBossVersion` from the store response (was silently defaulted), and the manual publish scripts read it from the jar's `plugin.json`; startup plugin passes are sequenced so the directory scan no longer races the persisted-plugin pass (eliminating the spurious "Plugin already loaded" failures and force-loading of disabled plugins), with new desktop tests for reject/offer/fail-open (#829). The host's own `plugins/plugin-api-core` copy of `McpServerController` gains `setPort` (added to `boss-plugin-api` 1.0.55, implemented by terminal-tab 2.5.9) so plugins referencing it no longer fail `BinaryCompatibilityValidator` at load - the validator statically scans bytecode, so a host-interface method (not a runtime guard) is the fix (#827). `.deb` maintainer scripts no longer abort under `set -e` when `xdg-*` desktop-integration tools fail on headless systems: `fixLinuxDesktopFile` appends an `\|\| echo …` fallback so missing integration degrades to a warning, verified in a headless `debian:12` container including self-heal over a broken half-install (#828); the xdg patch is hardened to be whitespace-tolerant across the whole `xdg-*` class and only repacks when the maintainer scripts changed (#830) |
| [v9.2.26](v9.2.26.md) | 2026-07-03 | Adds a public, key-free `latest-release` edge function (deployed with `verify_jwt=false`) over the `app_releases` catalog so download links can point at the newest installer per platform instead of a versioned asset: `GET /latest-release?app=boss\|bossterm` returns the latest release JSON, and `?download=dmg\|msi\|...` issues a `302` to the newest installer (optional `arch=arm64\|amd64` with `aarch64`/`x86_64`/`x64` aliases, plus `channel=`/`prerelease=true`). It reads the anon-readable table with the runtime anon key so browser links need no `apikey`, keeps the versioned filename on redirect, and sets `Cache-Control: no-store` so a cached `302` can't serve a stale installer; the asset picker handles naming quirks (release version required in filename, `Universal.dmg` for both Mac arches, un-suffixed `BOSS-x.y.z.msi` default, multi-arch Linux → amd64) and arch-filtered downloads degrade exact-arch → universal → un-suffixed default before 404ing (#822). The README Downloads table now links directly to those always-latest installers, split into per-arch rows (Windows x64 vs ARM64, Linux AMD64 vs ARM64), with all nine links verified live against v9.2.25, the releases page kept as the browse-all-versions fallback, and a JSON metadata link for scripts (#824) |
| [v9.2.25](v9.2.25.md) | 2026-07-03 | Fixes a startup race where "Last Session" restore ran before the dynamic plugins that own the browser/terminal/editor tab types had registered: restore always won (the workspace flow emits instantly while plugin install is disk/network-bound), so `TabRegistry.createTabComponent` found no factory and `addTab` silently dropped every restored tab - the same race dropped `PanelEventBus` open events (CodeBase/RunConfigurations on project selection). A new bounded (15s), listener-driven `awaitRegistryCondition()` (shared by `TabRegistry`/`PanelRegistry` consumers) makes `applyWorkspace()` wait for the tab types its layout needs before `clearAllPanels()` (the preserved-state fast path stays instant; a no-op once plugins are loaded), the panel-open collector waits for its panel to register, the fresh-install fallback no longer marks deep-link handlers ready while a restore is in flight, and `addTab` warns when it drops a tab for a missing factory. The Last Session `applyWorkspace` is wrapped in try/catch (rethrowing cancellation) so a throwing restore can't leave URL/file/terminal/workspace handlers unmarked, and `tabTypeIdFor` becomes the single source of truth for persisted-type → tab type id (#821) |
| [v9.2.24](v9.2.24.md) | 2026-07-03 | Lands the plugin-manager "Toolbox" rebrand in the host UI: the "Plugin Setup Wizard…" menu item becomes "Toolbox Setup Wizard…", the menu-bar "Plugin" panel list becomes "Toolbox" (with a "(No tools available)" fallback and a "Reload All Tools" Help action), and the setup wizard's copy - welcome hero (now the `HomeRepairService` glyph), step titles, category descriptions, install/complete summaries, and failure strings - switches from plugin(s) to tool(s); internal identifiers and log messages are unchanged (#819). Also renders the self-update dialog's "What's new" release notes as native Compose markdown (headings, bullet/numbered lists, GFM tables, fenced code blocks, thematic breaks, and inline bold/italic/code/strikethrough/clickable links) instead of raw plain text, with a best-effort fallback to the prior plain-text rendering, and stops the dialog resizing while scrolling - a fully composed `Column` + `verticalScroll` behind clamped intrinsics with pinned baselines fixes the height at `min(content, 220dp)` regardless of scroll position (#820) |
| [v9.2.23](v9.2.23.md) | 2026-07-03 | Moves BossEditor out of the host and into the `editor-tab` plugin - the same arrangement as BossTerm inside `terminal-tab`. `composeApp` drops `bosseditor-compose-desktop` and `kotlin-compiler-embeddable` (~60 MB off the host distributable); BossEditor now ships inside the `editor-tab` plugin JAR (>=1.4.0), which bundles the full PSI stack. A new `EditorTabPluginAPI` (mirroring `TerminalTabPluginAPI`) has the plugin serve the editor/LSP settings panels - host sections delegate through `EditorAPIAccess`, self-healing to the real panel once the plugin's async registration lands and showing a muted notice (not a blank panel) meanwhile. A `SystemPluginInfo.minVersion` gate force-updates an out-of-date `editor-tab` synchronously before load so the first launch can't `NoClassDefFoundError` on every editor tab (offline hosts keep the old JAR and retry). Deletes the dead host-side renderer (`BossEditorIntegration`, `DesktopCodeEditorUI`, `FileModificationTracker`, `EditorSearchEventBus`, `SemanticTokenProviderImpl`, `NavigationResolverProviderImpl`) and reduces `EditorContentProviderImpl` to file I/O, language detection, file-open routing, run configs, and font enumeration. Fixes: never install a `-thin.jar` release asset, verify the *downloaded* release satisfies `minVersion`, and a version comparator so `1.4.0-rc1` no longer satisfies `minVersion = "1.4.0"` (#818) |
| [v9.2.21](v9.2.21.md) | 2026-07-02 | Performance-focused release. Startup no longer indexes `user.dir` - a Finder-launched packaged app reported it as `/`, so the editor indexer walked the entire disk at ~100% CPU all session; editors now index the real project root on open. PSI warm-up, app-update Realtime, plugin JAR validation/classloading, and bundled-plugin/store disk work move off the main thread (shared supervised scope), and connectivity probes race primary + fallback for first success via `invokeOnCompletion` (worst case was 2×10 s before the first usable screen). `BossTheme` now remembers its typography so a fresh `FontFamily` per recomposition can't recompose the whole subtree. Bumps BossEditor to 1.0.4 (#817). Fixes split-divider dragging: `BossResizablePanel` stored a stale container size, so drags moved by a `newSize/oldSize` multiplier that silently reset on unrelated recompositions - panels now store a weight fraction against the live size, read bounds via `rememberUpdatedState`, and keep both sides ≥10% (#816). File scanning is faster and safer: `directoryHasChildren` streams and early-exits instead of materializing the full listing, `scanFileRecursively` stats each entry once before sorting, scans run on `Dispatchers.IO`, and `InvalidPathException` from malformed plugin paths degrades to `false` (#815) |
| [v9.2.20](v9.2.20.md) | 2026-07-02 | Introduces a plugin MCP tool framework: any dynamic plugin can contribute `mcp__boss__*` tools (via a new `McpTool` API + `McpServerController`, with `PluginContext.registerMcpToolProvider`/`mcpToolRegistry`) that appear while the plugin is active and disappear on disable/unload, with exposed tools = registered − user-disabled − permission-denied and per-tool `requiredPermissions`/`requiresAdmin` gating that `DynamicPluginManager` keeps in sync with live role changes; the registry serializes mutators under one lock, persists the disabled set atomically, and tears down partial registrations on failure (pairs with boss-plugin-api 1.0.51 + terminal-tab 2.5.0, adopted by 19 plugins, 27 new tests) (#814). Also fixes release publishing of oversized assets: files > 50 MB now upload through Supabase's TUS resumable endpoint in 6 MiB chunks with bounded retry, avoiding the HTTP 413 Cloudflare returns for BOSS's `.dmg` at its ~100 MB body cap (#813). Plus CI dependency bumps (actions/checkout 4→7, supabase/setup-cli 1→2) |
| [v9.2.19](v9.2.19.md) | 2026-06-30 | Makes Supabase the primary desktop self-update channel with Realtime push and an automatic GitHub Releases backup: an `UpdateSource` seam below `DesktopUpdateService` adds `SupabaseUpdateSource` (PostgREST over Ktor) and `GitHubUpdateSource` as a fallback, reusing the existing `GitHubRelease` model so version-selection/asset-matching is unchanged. `AppUpdateRealtimeService` subscribes to `app_releases` `postgres_changes` for immediate, push-based update checks (with reconnect catch-up), demoting the poll loop to a startup check plus disconnect-only safety net. Adds a public-read `app_releases` table and `app-releases` Storage bucket, optional SHA-256 download verification, single-flight startup-check coalescing, and bakes `SUPABASE_*` config into the packaged app from CI secrets (advances issue #33). Also documents the "Operator's Console" design system in the README with a committed visual styleguide (`docs/design-system.html`) (#810, #809) |
| [v9.2.18](v9.2.18.md) | 2026-06-30 | Introduces the "Operator's Console" design system - unified design tokens (palette, spacing, radius, elevation, motion, typography) shared by the host and terminal via the `BossTheme` accessor, with `BossColors` delegating to `BossPalette` so existing components re-skin to the amber signal automatically and MesloLGS wired as the mono brand voice. The host is theme-aware end-to-end: reactive tokens re-skin the whole app live, ~38 dialogs plus settings sections, panel title bar, and the tab-bar "+" button are migrated to semantic tokens, and a Settings theme picker offers three themes (Operator / Daylight / Clean) persisted to `~/.boss`. Browser content follows the host theme - `FluckEngine` mirrors it into the live JxBrowser engine via `Engine.setTheme(LIGHT/DARK)`. Public `BossTheme(content)` JVM signature is preserved for plugin binary compatibility; includes a startup init-order fix for `FluckEngine` (#808) |
| [v9.2.17](v9.2.17.md) | 2026-06-29 | Adds keyboard tab switching (Ctrl+Tab / Ctrl+Shift+Tab) within the active panel, wired through the existing keymap pipeline, with two cycling modes selectable in Settings > Shortcuts - Positional (tab-bar order, wrapping) and MRU (Alt+Tab style, committing on modifier release) - plus a centered, theme-aligned VS Code-style switcher HUD overlay for MRU; bindings are added to all keymap presets so `migrateSettings()` merges them into existing users' keymaps, and the defaulted `tabSwitchMode` field keeps old keymap files compatible (#805). Also gates plugin-store installs on a plugin's `requiredPermissions` (the same list the host uses to gate visibility/use after install, making it a single source of truth): `/download` returns 403 when a non-admin lacks the permission(s), the publish path persists it on create and both GitHub paths, and browse/detail/download responses expose it - backed by a `plugins.required_permissions text[]` migration recreating `get_plugin_with_stats` / `search_plugins`, with pgTAP coverage; empty lists stay open so legacy plugins remain installable (#806). Plus dependency bumps (protoc 4.35.1, gRPC, graalvm-native 1.1.3, maven-publish 0.37.0) |
| [v9.2.16](v9.2.16.md) | 2026-06-27 | Lets plugins introduce their own RBAC permissions: a manifest declares `definedPermissions: [{name, description}]` and the plugin-store edge function auto-registers each one (non-system, ungranted) at publish via the `register_plugin_permission()` SECURITY DEFINER RPC - namespaced (`domain.action`), reserved-domain-blocked (`role`/`user`/`api_key`/`rpa`/`secret`/`plugins`), never shadowing a system permission, never self-granting - with a `plugin_permissions` provenance table and pgTAP coverage, on both the GitHub and multi-step local-jar `/publish` paths (a dangling `requiredPermissions` entry is rejected). Also exposes installed-but-permission-hidden plugins to non-admins via `PluginLoaderDelegate.getInaccessiblePlugins()` (id, display name, missing permissions) so UIs can show an "ask an admin" banner instead of silently omitting them, and logs the precise missing-permission subset when a plugin is hidden (boss-plugin-api mirror 1.0.47) |
| [v9.2.15](v9.2.15.md) | 2026-06-27 | Adds a `secret.read` RBAC permission (granted to `admin` + `boss_admin`) so the Secret Manager panel can move off the legacy `requiresAdmin` flag onto the granular `requiredPermissions` model while its RPCs stay scoped to `auth.uid()`, restyles plugin sandbox toasts (`PluginToastHost`) to match BOSS dialogs, and batches in toolchain dependency bumps (Kotlin 2.4.0, Compose Multiplatform 1.11.1, JNA 5.19.1, Logback 1.5.34, JUnit Jupiter 6.1.0) |
| [v9.2.14](v9.2.14.md) | 2026-06-27 | Maintenance release with no functional code changes - a version bump on top of v9.2.13 to publish a fresh signed multi-platform build. All v9.2.13 functionality (granular RBAC role hierarchy with permission-based plugin gating, the `WindowManager` fit/restore window-resize API for embedded BossTerm panes, and the public email-link auth redirect fix) carries forward unchanged |
| [v9.2.13](v9.2.13.md) | 2026-06-27 | Introduces a granular RBAC role hierarchy: a new `role_hierarchy` table (parent inherits descendants' permissions) drives cycle-safe `get_role_descendants`/`get_effective_permissions`, `authorize()` walks the descendant closure, effective permissions ride the `user_permissions` JWT claim (magic-link + passkey), role-management RPCs are re-gated from the literal admin role to `authorize('role.<verb>')`, delegation is tree-derived (grant only strictly below yourself), and host plugins declare `requiredPermissions` so `DynamicPluginManager` gates registration - plus a security-review fix gating the "read all users" RLS on `role.read` instead of the baseline-carried `user.read`. Also adds a `WindowManager` fit/restore window-resize API (`fitWindowByDelta`/`restoreWindowSize`, HiDPI-aware, screen-clamped, no-op until called) so embedded BossTerm panes can resize the host OS window for "Fit host to my screen", and sets `verify_jwt=false` on the email-link redirect edge function so the public verification link can reach it |
| [v9.2.12](v9.2.12.md) | 2026-06-20 | Adds BossTerm magic-link sign-in to the shared auth backend (per-app email/page branding via a `redirect_to == bossterm://auth/verify` signal, a `bossterm://auth/verify` deeplink, and verbatim `type` passthrough so a new user's first link verifies as `signup`), refactors the redirect edge function into an edge-runtime-safe `app.ts`/`index.ts` split with a deno test suite, and closes a pre-existing reflected-XSS hole in the redirect page by output-escaping the deeplink per context. Also adds a host JS↔Java bridge for co-browse tab sharing and emits host system events onto the application event bus. Fixes the branded-Chromium `windows-x64` build hang (plus an incomplete-release guard), terminal launches in folders with spaces (shell-quoted `{projectPath}`), and the update dialog growing vertically while scrolling release notes |
| [v9.2.11](v9.2.11.md) | 2026-06-06 | Fixes a destructive startup cycle between the `boss-plugin-terminal` and `boss-plugin-terminal-tab` system plugins. Because the former's artifact name is a filename prefix of the latter, two cleanup/version-check sites in `PluginStoreSetup` (`copyBundledPluginsToPluginDir`'s double-`substringBeforeLast("-")` prefix derivation and `ensureSystemPluginsInstalled`'s `startsWith(artifactPrefix)` post-download cleanup) repeatedly deleted each other's JARs on launch. Both sites now match candidates by reading each JAR's manifest plugin ID via `readPluginManifest` (which leaves unreadable/non-plugin JARs untouched); the `downloadOnly` lookup keeps its prefix match since it only runs for the collision-free microkernel runtime |
| [v9.2.10](v9.2.10.md) | 2026-06-05 | Makes `BrowserService` the single browser entry point for plugins - folds browser automation in (replacing the separate `RpaBrowserProvider`) with opt-in managed profiles (cookie/header auth seeding, ephemeral + persistent named profiles, per-profile fencing, LRU disk-cap eviction), `executeJavaScript`/`loadUrlAndWait`, and new tab-creation hooks, while keeping `BrowserConfig` binary-compatible with pre-built plugins; mirrors boss-plugin-api 1.0.42 and fixes a managed-profile leak/deadlock plus a stale per-tab integration cache. Also adds a dismissible update dialog with persistent per-version dismissal (`~/.boss/update-settings.json`, single-window reactive ownership), and a `PluginJarReconciler` that groups duplicate JARs by manifest plugin ID and keeps the highest version so a stale JAR can no longer shadow a newer one at startup. Plus dependency bumps (gradle-wrapper 9.5.1, Netty 4.2.15, protoc 4.35.0, actions/add-to-project v2) |
| [v9.2.9](v9.2.9.md) | 2026-06-04 | Brings sidebar plugins to parity with main tabs: a header right-click menu (Restart Panel / Reload Plugin / Open as Tab / Minimize from one shared list), "Open as Tab" that *moves* a plugin into a focused main tab (single cached instance, state preserved; icon refocuses, closing the last host tab restores it to the sidebar), a draggable header that reorders in-sidebar or promotes to a tab with split-pane detection, and a per-plugin "Update" badge + "Check for Updates". Also hardens workspace restore (skip unknown/legacy tab types instead of throwing "Unknown tab type"; no ghost split on recovery), enforces single-instance panel-host tabs via a true reference count + ownership-checked unregister, makes `PluginUpdateRegistry` atomic, and sanitizes the plugin-update download path |
| [v9.2.8](v9.2.8.md) | 2026-06-03 | Fixes the auto-update progress bar jumping from 0% straight to 100% by switching `downloadUpdate()` to a streaming download - `prepareGet().execute { }` + `bodyAsChannel()` via a shared `streamToFile()` helper (the pattern already used in `RemotePluginRepository`) instead of Ktor's buffering `get()` (which read the whole body into memory before the progress loop ran). Also replaces the buggy unknown-size sawtooth fallback with a monotonic asymptotic curve; no UI/state changes |
| [v9.2.7](v9.2.7.md) | 2026-06-03 | Fixes "Restart BOSS" silently quitting without relaunching on packaged (jpackage) installs - the path the plugin manager's post-update restart prompt invokes. `ApplicationRestarter.scheduleRestart()` relaunches the native install (macOS `.app` via LaunchServices; Windows/Linux jpackage launcher re-exec) instead of `java -jar` (which can't boot a packaged app), waiting for the old PID to exit first; Windows uses a PowerShell `Get-Process` wait loop to avoid racing `SingleInstanceManager`, and `detectMacAppBundle` validates candidates with `File.exists()` |
| [v9.2.6](v9.2.6.md) | 2026-06-03 | Lets the plugin manager apply a plugin update to its already-open instances: new `PluginLoaderDelegate` entries (`getRunningInstanceCount`, `resetPluginInstances`, `restartApplication`) count, reset (reload + close *all* of a plugin's open tabs across panels/windows on the EDT before reloading, via `SplitViewStateRegistry` + `TabSandboxRegistry`), or restart through `ApplicationRestarter.scheduleRestart()`, so a freshly-updated plugin no longer keeps running old code in background tabs. Also fixes `PluginCrashRegistry.unregisterActiveTab(tabId)` so an older tab's dispose can't wipe a newer tab's mapping. Pairs with boss-plugin-api 1.0.39 and plugin-manager 1.4.44 |
| [v9.2.5](v9.2.5.md) | 2026-06-02 | Two runner improvements: Claude/MCP-initiated sidebar runs (via the `run_in_sidebar` tool) now reflect in the top-bar runner - added to history, selected in the dropdown, and marked running via the new `registerSidebarRun()` host entry - and `resolveWorkingDirectory()` re-derives each run's working directory from the script's source `filePath` at execution time, so runs always start at the script's project root instead of a stale or workspace-root path |
| [v9.2.4](v9.2.4.md) | 2026-06-02 | Plugin loading + validator fixes: bundled-plugins scanner uses the correct `boss_plugins` (plural) path and the background updater no longer downgrades locally-built plugins ahead of the published release, and `BinaryCompatibilityValidator` only enforces the `ai.rever.boss.plugin.*` contract - signature drift in a plugin's *bundled* third-party libs (ktor → kotlinx-serialization) no longer disables the plugin at load. Also reverts ktor to 3.4.3 + serialization to 1.9.0 to match the MCP SDK stack (terminal-tab's bundled MCP server broke on 3.5.0/1.11.0) |
| [v9.2.3](v9.2.3.md) | 2026-06-01 | Two focus/path fixes: clicked splits keep focus when another split emits output (AWT-backed children don't bubble Compose focus events, and `buildList` was calling `setActivePanel` on every recomposition), and auto-detected run configs `cd` to the script's project root so `./gradlew :module:run` resolves when the active workspace differs from the script's location |
| [v9.2.2](v9.2.2.md) | 2026-05-30 | IPC compatibility hardening: per-version `min_ipc_version` plumbed end-to-end through the plugin store, host system-plugin auto-updater gated by `IpcVersion.CURRENT` (with the latent reflection no-op fixed), `IpcVersion.CURRENT` and `:boss-ipc` module version both rolled to 1.0.0 to match what `TerminalServiceImpl` implements after the in-process pivot, sandbox health-metrics race fixed under coroutines 1.11.0, plus Ktor 3.5.0 / protobuf 4.35.0 / kotlinx-coroutines 1.11.0 / JxBrowser plugin 2.1.0 bumps |
| [v9.2.0](v9.2.0.md) | 2026-05-29 | Pivot terminal-OOP strategy: drop `bossterm-compose` from the host classloader entirely (plugin bundles it privately inside `boss-plugin-terminal-tab` v2.1.0+ and runs via parent-classloader delegation), retire the dormant Phase A/B/C renderer scaffolding from `main` while keeping the additive `boss-ipc` 1.1.0 proto, and tighten `BinaryCompatibilityValidator` to soft-fail OOP runtime-package refs and stop flagging `Object`-method calls dispatched through interface receivers |
| [v9.1.11](v9.1.11.md) | 2026-05-22 | First three phases of moving the terminal plugin out-of-process: additive `boss-ipc` 1.0.0 → 1.1.0 contract with grid/cursor/scrollback/shell-event streams, host-side pure-Compose `TerminalGridRenderer` (with cell-run coalescing) behind `boss.terminal.oopRenderer` flag, and `TerminalGridConnection` gRPC scaffolding (connect/adopt) ready for Phase D wiring - default behaviour unchanged |
| [v9.1.10](v9.1.10.md) | 2026-05-20 | Background auto-update for non-downloadOnly system plugins (plugin-manager, terminal-tab, fluck-browser, editor-tab, terminal, boss-plugin-api) so long-running installs stop pinning to first-launch versions, and a `Key.Home` → `Key.MoveHome` fix that unblocks Compose Multiplatform 1.11.0 (also bumps BossTerm 1.1.96, grpc-kotlin 1.5.0, androidx-lifecycle 2.10.0, Netty 4.2.13, slf4j 2.0.18) |
| [v9.1.8](v9.1.8.md) | 2026-05-14 | Customize Sidebar menu with per-slot hide/show submenus, draggable three-dot button that relocates between all five sidebar slots, View → Customize Sidebar… entry, and a main-panel terminal focus fix via the new `LocalIsPanelActive` composition local (BossTerm 1.0.94) |
| [v9.1.6](v9.1.6.md) | 2026-05-12 | Auto-trigger public-repo release sync via `workflow_run` fallback with timestamp-gated discovery and a static global concurrency group, plus dependency bumps (Ktor 3.4.3, Supabase 3.6.0, GraalVM Native Build Tools 1.1.0, kotlinx-datetime 0.8.0, BossTerm 1.0.93) |
| [v9.1.5](v9.1.5.md) | 2026-05-12 | Queue concurrent form-submit POST stashes FIFO and make stash/consume atomic under `ConcurrentHashMap.compute`, fixing the double-print regression where a second OncoEMR Print before the first tab finished constructing would clobber the first popup's body |
| [v9.1.4](v9.1.4.md) | 2026-05-12 | Preserve POST body when form-submit popups (`target="_blank"`) become BOSS tabs, fixing OncoEMR Print and similar form-POST + new-window flows that previously hit the server with an empty GET |
| [v9.1.3](v9.1.3.md) | 2026-05-09 | Top-bar git state auto-refreshes on external HEAD changes (CLI checkout, rebase) via coalesced NIO watcher, and the plugin install wizard no longer tries to install the microkernel runtime |
| [v9.1.2](v9.1.2.md) | 2026-05-08 | Microkernel runtime extracted from tree and distributed as a versioned plugin via GitHub releases with IPC version compat gating, JxBrowser 9.0.2 with branding patch fixes, BOSS Lite release workflow, and major dependency updates (Kotlin 2.3.21, gRPC 1.81.0, protobuf 4.34.1, Gradle 9.5.0) |
| [v8.17.24](v8.17.24.md) | 2026-03-12 | Windows ARM64 build fix: exclude OOP plugin files from compilation on ARM64 |
| [v8.17.23](v8.17.23.md) | 2026-03-12 | Compiler warnings cleanup across 10 files and five dependency updates including Compose Multiplatform 1.10.2, Ktor 3.4.1, and Netty 4.2.x |
| [v8.17.22](v8.17.22.md) | 2026-03-12 | Full split-brain microkernel OOP isolation with 15 gRPC service bridges, parallel plugin spawning (16x faster startup), configurable heap, and comprehensive thread-safety fixes |
| [v8.17.21](v8.17.21.md) | 2026-03-09 | Windows ARM64 build fix: exclude microkernel modules where protoc is unavailable, with reflection-based KernelBootstrap loading |
| [v8.17.20](v8.17.20.md) | 2026-03-08 | Microkernel architecture foundation with gRPC IPC, AI-powered self-healing orchestration, and Supabase Realtime stability fixes |
| [v8.17.19](v8.17.19.md) | 2026-03-08 | Fix intra-JAR false positives in plugin binary compatibility validator |
| [v8.17.18](v8.17.18.md) | 2026-03-07 | Plugin binary compatibility validation at load time with Compose layout reentrancy crash fix and corrected NoSuchMethodError detection |
| [v8.17.17](v8.17.17.md) | 2026-03-07 | Documentation maintenance release adding missing release notes for v8.17.16 |
| [v8.17.16](v8.17.16.md) | 2026-03-07 | Smart file routing opening media in browser and code in editor, "Open With" context menu, plugin crash recovery for disabled and side panel plugins, and Windows path handling fix |
| [v8.17.15](v8.17.15.md) | 2026-03-07 | Database performance and security hardening with optimized Postgres views for user/role and plugin store queries, RLS bypass fix, and anonymous access revocation |
| [v8.17.14](v8.17.14.md) | 2026-03-07 | Critical fix restoring SupabaseDataProvider access for sandboxed plugins via SandboxedPluginContext delegation |
| [v8.17.13](v8.17.13.md) | 2026-03-07 | Plugin API expansion adding generic SupabaseDataProvider for Postgrest queries without Supabase SDK imports |
| [v8.17.12](v8.17.12.md) | 2026-03-07 | Documentation maintenance release adding missing release notes for v8.17.11 |
| [v8.17.11](v8.17.11.md) | 2026-03-06 | macOS default browser detection fixes: reliable Swift-based handler lookup, null-safe check, and platform-aware instructions dialog |
| [v8.17.10](v8.17.10.md) | 2026-03-06 | Auto-disable incompatible plugins with crash recovery and cleaner New Tab dialog hiding disabled plugin tabs |
| [v8.17.8](v8.17.8.md) | 2026-03-05 | Dead Fluck browser UI removed (~8k lines) and browser infrastructure consolidated into plugin/browser package |
| [v8.17.7](v8.17.7.md) | 2026-03-05 | Plugin crash sandboxing with PluginCrashRegistry, centralized .boss data directory via BossDirectories, and BossEditor migrated to Maven artifact |
| [v8.17.6](v8.17.6.md) | 2026-03-02 | macOS fullscreen close fix, deprecated plugin-api module removed, and CI/CD dependency updates |
| [v8.17.5](v8.17.5.md) | 2026-02-21 | Complete plugin API parity closing all 42 gaps across editor, terminal, and platform categories, plus installed plugin version tracking fix |
| [v8.17.4](v8.17.4.md) | 2026-02-19 | Browser find-in-page (Cmd+F) and paste-without-formatting (Cmd+Shift+V), Supabase Realtime for live plugin store updates, and HYBRID plugin type fix |
| [v8.17.3](v8.17.3.md) | 2026-02-16 | Critical macOS build fix ensuring proper Chromium binary extraction with preserved symlinks for stable browser functionality |
| [v8.17.2](v8.17.2.md) | 2026-02-13 | Documentation update release adding missing release notes for v8.16.45 |
| [v8.17.1](v8.17.1.md) | 2026-02-15 | Stability-focused release reverting WebAuthn entitlements and JxBrowser 8.17.0 upgrade to restore stable state while features undergo additional testing |
| [v8.16.44](v8.16.44.md) | 2026-02-12 | RPA recorder plugin browser support and browser profile cleanup improvements for better resource management and automation capabilities |
| [v8.16.43](v8.16.43.md) | 2026-02-12 | Critical plugin memory leak fix ensuring proper cleanup of dynamic plugin browser instances when windows are closed |
| [v8.16.42](v8.16.42.md) | 2026-02-12 | Critical stability fix preventing macOS crash when closing the application while in fullscreen mode |
| [v8.16.40](v8.16.40.md) | 2026-02-12 | Terminal sidebar plugin stability improvements with bundled distribution and enhanced GitHub URL integration |
| [v8.16.38](v8.16.38.md) | 2026-02-12 | Plugin system stability improvements and significant distribution size reduction with ~86MB saved by removing bundled JxBrowser platform binary |
| [v8.16.36](v8.16.36.md) | 2026-02-12 | Terminal plugin architecture refactoring with hot-reload support for improved modularity and developer experience |
| [v8.16.34](v8.16.34.md) | 2026-02-12 | Critical terminal tab fix ensuring terminals open in the correct project directory |
| [v8.16.33](v8.16.33.md) | 2026-02-12 | Build system improvement bundling the bookmarks plugin directly into CI releases |
| [v8.16.32](v8.16.32.md) | 2026-02-09 | Build system reliability improvements with task dependency fixes and Kotlin platform updates |
| [v8.16.30](v8.16.30.md) | 2026-02-08 | Plugin system enhancements with project selection API and minimum version validation for safer plugin installations |
| [v8.16.29](v8.16.29.md) | 2026-02-08 | Maintenance release adding screen capture support for browser plugins with enhanced permission APIs |
| [v8.16.28](v8.16.28.md) | 2026-02-08 | Stable release introducing mandatory tab plugins for streamlined setup and improved Windows CI/CD reliability |
| [v8.16.27-alpha.1](v8.16.27-alpha.1.md) | 2026-02-07 | Major plugin API enhancements for dynamic tab providers and improved browser URL handling for better navigation experience |
| [v8.16.26](v8.16.26.md) | 2026-02-06 | Enhanced plugin wizard with automatic triggering when no plugins are installed and improved modal dialog experience |
| [v8.16.25](v8.16.25.md) | 2026-02-06 | Stable release promoting the plugin install wizard with Help menu access and threading improvements |
| [v8.16.24-alpha.1](v8.16.24-alpha.1.md) | 2026-02-06 | Alpha release introducing interactive plugin setup wizard for streamlined first-time user onboarding |
| [v8.16.23-alpha.1](v8.16.23-alpha.1.md) | 2026-02-05 | Alpha release focused on dynamic plugin architecture with Maven Central publishing and runtime plugin management |
| [v8.16.22](v8.16.22.md) | 2026-01-31 | Maintenance release adding plugin sandbox for improved stability and crash isolation |
| [v8.16.21](v8.16.21.md) | 2026-01-30 | Maintenance release adding refactoring capabilities to BossEditor with Rename support |
| [v8.16.20](v8.16.20.md) | 2026-01-30 | Stable release featuring critical CodeBase panel fixes and enhanced CI/CD workflow capabilities |
| [v8.16.19-alpha.1](v8.16.19-alpha.1.md) | 2026-01-30 | Alpha release focused on Gradle 9 compatibility and CI/CD workflow improvements for enhanced build reliability |
| [v8.16.18](v8.16.18.md) | 2026-01-29 | CI/CD improvements and security hardening for release workflow with global search and file operations context menu |
| [v8.16.15](v8.16.15.md) | 2026-01-27 | Critical browser focus fix eliminating interruptions from 100ms polling in split view |
| [v8.16.14](v8.16.14.md) | 2026-01-27 | Dependency maintenance release modernizing build configuration with direct Maven coordinates |
| [v8.16.13](v8.16.13.md) | 2026-01-27 | Documentation maintenance release with comprehensive release notes for v8.16.12 |
| [v8.16.12](v8.16.12.md) | 2026-01-25 | Dashboard metrics integration and plugin architecture refactoring with 31 separate Gradle modules |
| [v8.16.11](v8.16.11.md) | 2026-01-25 | Browser tab fullscreen mode with improved video rendering for immersive viewing experience |
| [v8.16.10](v8.16.10.md) | 2026-01-25 | Git repository cloning with authentication support and crash dialogs in separate windows for UI thread crashes |
| [v8.16.9](v8.16.9.md) | 2026-01-25 | Keyboard shortcuts for split tab operations, Google search integration in context menus, and unified context menu styling |
| [v8.16.8](v8.16.8.md) | 2026-01-25 | Structured logging framework with sensitive data sanitization, macOS trackpad scroll fix, editor optimization, and discrete password fill mode |
| [v8.16.7](v8.16.7.md) | 2026-01-24 | Scrollbar settings fix ensuring visibility preferences properly apply to all panels |
| [v8.16.6](v8.16.6.md) | 2026-01-24 | UI polish improvements with panel scrollbars, keyboard input fixes, and bottom bar refinements for cleaner, more functional interface |
| [v8.16.5](v8.16.5.md) | 2026-01-24 | Keyboard shortcut reliability fix with MenuBar as single source of truth, eliminating duplicate handlers and improving cross-platform consistency |
| [v8.16.4](v8.16.4.md) | 2026-01-23 | Critical multi-window state management fixes: removed global project and runner configuration state for independent window operation |
| [v8.16.3](v8.16.3.md) | 2026-01-23 | Enhanced keyboard shortcut system with BossTerm patterns and consistent dark theme across settings |
| [v8.16.1](v8.16.1.md) | 2026-01-23 | JxBrowser 8.16.1 (Chromium 144) upgrade with centralized version management and workflow fixes |
| [v8.16.0](v8.16.0.md) | 2026-01-23 | Comprehensive video playback support and chrome:// URL navigation with proprietary codecs and Widevine DRM |
| [v8.15.28](v8.15.28.md) | 2026-01-23 | Fixed split view resize cursor interfering with JxBrowser functionality |
| [v8.15.27](v8.15.27.md) | 2026-01-23 | macOS fullscreen fix with transparent title bar restoration and major README overhaul with badges and quick install commands |
| [v8.15.26](v8.15.26.md) | 2026-01-22 | Comprehensive event bus architecture update with sourceWindowId support across all 11 event buses for proper multi-window event routing |
| [v8.15.25](v8.15.25.md) | 2026-01-22 | Multi-window event filtering fixes for runner terminal, hyperlink dialogs, and browser tab titles |
| [v8.15.24](v8.15.24.md) | 2026-01-21 | Critical multi-window crash fix and BossTerm 1.0.87 update |
| [v8.15.23](v8.15.23.md) | 2026-01-21 | Browser scroll lag fix, improved focus mode responsiveness, file tree loading fixes, and Linux update enhancements |
| [v8.15.22](v8.15.22.md) | 2026-01-20 | Title bar hover reveal in focus mode, search functionality for large file viewer, and logging improvements |
| [v8.15.21](v8.15.21.md) | 2026-01-20 | Memory-efficient large file viewer for BossEditor and automatic update fallback for expired tokens |
| [v8.15.20](v8.15.20.md) | 2026-01-19 | Critical CI/CD workflow fix restoring all 5 platform build configurations |
| [v8.15.19](v8.15.19.md) | 2026-01-19 | Critical macOS ARM64 performance fix and comprehensive dependency updates |
| [v8.15.18](v8.15.18.md) | 2026-01-12 | Windows path handling fixes and browser engine download messaging improvements |
| [v8.15.17](v8.15.17.md) | 2026-01-12 | Fixed folder loading stuck during codebase indexing |
| [v8.15.16](v8.15.16.md) | 2026-01-11 | Critical macOS ARM64 stability fix and CI/CD reliability improvements |
| [v8.15.11](v8.15.11.md) | 2026-01-11 | Browser zoom controls, Git integration in top bar, enhanced URL bar autocomplete, and Windows ARM64 Chromium branding support |
| [v8.15.10](v8.15.10.md) | 2026-01-11 | Chromium notification branding fixes and Linux/FreeBSD terminal support improvements |
| [v8.15.9](v8.15.9.md) | 2026-01-09 | Tab management improvements: fixed drag reorder positioning, added auto-scroll, and resolved macOS Chromium permissions |
| [v8.8.0](v8.8.0.md) | 2024-10 | Initial public release with integrated Fluck browser, terminal support, multi-panel layouts, and LLM-powered RPA automation |
| [v8.8.0-windows-msi](v8.8.0-windows-msi.md) | 2024-10 | Windows-specific release adding professional MSI installer and standalone distribution packaging with icon support and comprehensive build tooling |
| [v8.11.4](v8.11.4.md) | 2024-10 | Critical stability fix for Apple Silicon users and improvements to GitHub Actions release workflow automation |
| [v8.9.8](v8.9.8.md) | 2024-10 | Maintenance release with GitHub Actions workflow improvements for release asset synchronization and repository cleanup |
| [v8.9.7](v8.9.7.md) | 2024-10 | Maintenance release with GitHub Actions workflow improvements for release asset synchronization and repository cleanup |
| [v8.9.6](v8.9.6.md) | 2024-10 | Maintenance release with GitHub Actions workflow improvements for release asset synchronization and repository cleanup |
| [v8.9.5](v8.9.5.md) | 2024-10 | Maintenance release with GitHub Actions workflow improvements for release asset synchronization and repository cleanup |
| [v8.9.4](v8.9.4.md) | 2024-10 | Maintenance release with GitHub Actions workflow improvements for release asset synchronization and repository cleanup |
| [v8.9.2](v8.9.2.md) | 2024-10 | Maintenance release with GitHub Actions workflow improvements for release asset synchronization and repository cleanup |
| [v8.9.1](v8.9.1.md) | 2024-10 | Complete auto-update implementation with improved UI and progress tracking, along with GitHub Actions workflow refinements |
| [v8.9.0](v8.9.0.md) | 2024-10 | GitHub Actions workflow modernization with Slack notifications, comprehensive dependency updates, and workflow consolidation for improved CI/CD efficiency |
| [v8.8.1](v8.8.1.md) | 2024-10 | Windows code signing infrastructure with DigiCert KeyLocker integration, streamlined GitHub Actions workflows, and comprehensive CI/CD reliability improvements |
| [v8.11.2](v8.11.2.md) | 2024-10 | Major authentication system overhaul with complete Supabase integration, enhanced macOS code signing for notarization compliance, and framework updates |
| [v8.10.1](v8.10.1.md) | 2024-10 | Linux packaging support with optimized distribution formats, streamlined GitHub Actions workflows, and dependency updates |
| [v8.10.0](v8.10.0.md) | 2024-10 | Windows code signing infrastructure with DigiCert KeyLocker integration, streamlined GitHub Actions workflows, and comprehensive CI/CD reliability improvements |
| [v8.12.12](v8.12.12.md) | 2024-10 | Secret management with user-level viewing, role-based sharing with audit trails, and database migration consolidation |
| [v8.12.6](v8.12.6.md) | 2024-10 | Removed unused Supabase demo UI components and settings dialogs while preserving all core Supabase functionality |
| [v8.12.5](v8.12.5.md) | 2024-10 | Admin Role Management plugin with user management, server-side search, pagination, role assignment/removal, and user deletion |
| [v8.12.4](v8.12.4.md) | 2024-10 | Role-Based Access Control (RBAC) system with Supabase Custom Claims integration and JWT-based authentication for both magic link and passkey flows |
| [v8.12.3](v8.12.3.md) | 2024-10 | Configuration system refactored to workspaces with automatic tab cleanup when workspaces are deleted |
| [v8.12.2](v8.12.2.md) | 2024-10 | Passkey authentication fixes with proper rpId configuration, magic link deep linking support, and dependency updates |
| [v8.12.1](v8.12.1.md) | 2024-10 | Critical macOS code signing and notarization fixes to ensure proper distribution and security compliance |
| [v8.12.0](v8.12.0.md) | 2024-10 | Simplified WebAuthn authentication, cross-device passkey support, Apple Silicon hardened runtime, and major dependency updates |
| [v8.12.11](v8.12.11.md) | 2024-10 | Fixed bottom bar to correctly display and update with active tabs, and cleaned up top bar by hiding unimplemented components |
| [v8.12.10](v8.12.10.md) | 2024-10 | Plugin architecture cleanup and database schema consolidation with critical security fixes |
| [v8.12.9](v8.12.9.md) | 2024-10 | Secret Manager admin plugin with server-side encryption and critical RBAC fixes |
| [v8.12.8](v8.12.8.md) | 2024-10 | Dynamic RBAC system with runtime role/permission management, ENUM-to-table migration, and passkey refresh token fix |
| [v8.12.7](v8.12.7.md) | 2024-10 | Admin role and permission creation panel with ENUM-to-table RBAC migration and full CRUD operations |
| [v8.12.16](v8.12.16.md) | 2024-10 | Fixed critical macOS update installation failure and hardened update script security against command injection vulnerabilities |
| [v8.12.15](v8.12.15.md) | 2024-10 | User feedback for expired and already-used magic links, eliminating silent authentication failures |
| [v8.12.14](v8.12.14.md) | 2024-10 | Fixed embedded browser for Settings passkey registration and added auto-click capability |
| [v8.12.13](v8.12.13.md) | 2024-10 | Improved passkey selection UX with better credential identifiers and fixed error message cross-contamination between authentication flows |
| [v8.12.19](v8.12.19.md) | 2024-10 | Cross-platform default browser support allowing BOSS to be set as the system's default web browser with automatic URL handling |
| [v8.12.18](v8.12.18.md) | 2025-10 | Multiple window support with independent instances, browser-standard keyboard shortcuts, and event-driven focus management |
| [v8.12.17](v8.12.17.md) | 2025-10 | Optional automatic update checking with configurable intervals and improved time display accuracy |
| [v8.12.23](v8.12.23.md) | 2024-10 | Critical browser stability fixes: resolved JxBrowser SIGSEGV crashes and auto-window creation bug on macOS |
| [v8.12.22](v8.12.22.md) | 2024-12 | Terminal rendering improvements and deep link handler fixes: resolved blank display bug, empty line rendering, and external URL handling when BOSS is set as default browser |
| [v8.12.21](v8.12.21.md) | 2024-10 | Secret Auto-Fill Integration with Fluck browser, enabling seamless credential management with real-time synchronization across all components |
| [v8.12.24](v8.12.24.md) | 2024-12 | Native macOS MenuBar integration for improved platform-native look and feel |
| [v8.12.26](v8.12.26.md) | 2025-12 | Interactive home screen with actionable tip cards and URL event bus for proper external link handling |
| [v8.12.25](v8.12.25.md) | 2025-12 | Plugin lifecycle system with reset functionality and tab cloning fix |
| [v8.12.29](v8.12.29.md) | 2025-12 | Enhanced form autofill with React 16+ compatibility and proper event dispatching |
| [v8.12.28](v8.12.28.md) | 2025-12 | Network reliability improvements with separate HttpClient configurations for API calls and downloads with optimized timeouts |
| [v8.12.27](v8.12.27.md) | 2025-12 | Fixed hyperlinks opening in detached OS windows by intercepting popups to open as BOSS tabs instead |
| [v8.12.31](v8.12.31.md) | 2025-12 | Browser tab state management and favicon loading reliability improvements with URL bar isolation and enhanced caching |
| [v8.12.30](v8.12.30.md) | 2025-12 | Console panel improvements with complete log history, Fluck-style UI, multi-line text selection, and Terminal emoji support |
| [v8.13.0](v8.13.0.md) | 2025-11 | Complete Bookmarks Plugin with favorites, collections, search, multi-workspace targeting, and comprehensive context menus |
| [v8.13.2](v8.13.2.md) | 2025-11 | Intelligent popup handling for OAuth and mail links with smart detection and proper window management |
| [v8.13.1](v8.13.1.md) | 2025-11 | Fixed tab bar layout to span full width of main panel |
| [v8.13.10](v8.13.10.md) | 2025-11 | CI/CD reliability improvements with fixed duplicate tag creation in release workflow |
| [v8.13.9](v8.13.9.md) | 2025-11 | CI/CD reliability improvements: prevented race conditions in tagging and consolidated workflow to eliminate duplicate runs |
| [v8.13.8](v8.13.8.md) | 2025-11 | Maintenance release with no functional changes |
| [v8.13.7](v8.13.7.md) | 2025-11 | Fixed critical macOS notarization issue that prevented proper app signing and distribution |
| [v8.13.6](v8.13.6.md) | 2025-11 | Comprehensive CLI functionality with Homebrew installation support, smart argument detection, and automatic version management |
| [v8.13.5](v8.13.5.md) | 2025-11 | Fixed keyboard shortcuts triggering while typing in browser input fields |
| [v8.13.4](v8.13.4.md) | 2025-11 | New keyboard shortcuts management tab in Settings for comprehensive view and control of all application shortcuts |
| [v8.13.3](v8.13.3.md) | 2025-11 | Version selection and downgrade support, Top of Mind plugin enhancements with nested sections and favicons, and dynamic tab bar improvements |
| [v8.13.18](v8.13.18.md) | 2024-12 | Fixed browser URL bar regression where Enter key navigated to autocomplete suggestion instead of typed text |
| [v8.13.17](v8.13.17.md) | 2024-12 | Build system stability fixes, browser URL bar usability improvements, and macOS build dependency resolution |
| [v8.13.14](v8.13.14.md) | 2024-12 | Browser usability enhancements with favicons in quick switcher, faster link clicks, URL bar text fixes, and authentication infrastructure cleanup |
| [v8.13.12](v8.13.12.md) | 2024-12 | Focus Mode with keyboard shortcuts for distraction-free work by hiding UI panels |
| [v8.13.11](v8.13.11.md) | 2024-12 | Authentication stability, adaptive window sizing for laptops, standardized search bars, and CLI improvements |
| [v8.13.32](v8.13.32.md) | 2025-12 | Performance monitoring system, IntelliJ-style run configurations with smart detection, and project persistence across sessions |
| [v8.13.36](v8.13.36.md) | 2025-12 | Drag-and-drop support for tabs: reorder within windows and move between windows, with improved lifecycle management |
| [v8.13.35](v8.13.35.md) | 2025-12 | Fixed critical context menu actions bug in tabs caused by trailing lambda parameter ordering issue |
| [v8.13.34](v8.13.34.md) | 2025-12 | Browser stability improvements with automatic recovery, configurable settings, and terminal project folder support |
| [v8.13.33](v8.13.33.md) | 2025-12 | Critical browser stability improvements: Reset Browser feature, thread safety fixes, and streamlined Linux update authentication |
| [v8.14.10](v8.14.10.md) | 2025-12 | Improved new user experience with better project selection workflows and updated plugin icons |
| [v8.14.9](v8.14.9.md) | 2025-12 | New Dashboard feature with recent items and templates, HTTP client memory leak fix, and performance optimizations |
| [v8.14.8](v8.14.8.md) | 2025-12 | BossEditor Phase 19 polish, terminal file hyperlink line:column navigation, browser URL recovery fixes, and focus mode default changes |
| [v8.14.7](v8.14.7.md) | 2026-01 | Maintenance release with no functional changes |
| [v8.14.6](v8.14.6.md) | 2025-12 | Offline screen with retry functionality on startup and terminal integration updates |
| [v8.14.5](v8.14.5.md) | 2025-12 | BossEditor improvements: navigation feedback, large document performance optimization, enhanced error handling, and comprehensive test coverage |
| [v8.14.4](v8.14.4.md) | 2025-12 | BossEditor enabled by default with complete custom editor implementation: LSP integration, code completion, multi-caret editing, minimap, rainbow brackets, and 40+ syntax lexers |
| [v8.14.3](v8.14.3.md) | 2025-12 | Enhanced terminal link handling with new "Existing Split" option for opening links in specific split locations |
| [v8.14.2](v8.14.2.md) | 2025-12 | Browser stability improvements: BOSS-styled JavaScript dialogs, file:// URL support, and fixed mouse button handling |
| [v8.14.1](v8.14.1.md) | 2025-12 | Terminal link click prompts with preferences, Reset Terminal feature, enhanced New Tab Dialog with file browsing, and browser error handling improvements |
| [v8.14.0](v8.14.0.md) | 2025-12 | Runner terminal integration with sidebar panel, unified execution service, and play/stop controls |
| [keylocker-tools](vkeylocker-tools.md) | 2025-07-28 | CI/CD workflow improvements and repository cleanup |
| [v8.15.8](v8.15.8.md) | 2026-01-07 | Browser navigation stability, Linux ARM64 support, UI improvements, and 139 build warnings resolved |
| [v8.15.7](v8.15.7.md) | 2026-01-06 | Fixed sidebar panel positioning for bottom slots |
| [v8.15.6](v8.15.6.md) | 2026-01-06 | AI integration: BossTerm AI Assistant with Welcome Wizard and terminal context menu for AI coding assistants |
| [v8.15.5](v8.15.5.md) | 2026-01-06 | Project creation wizard with templates, improved deleted project handling, and terminal AI assistant context menu |
| [v8.15.4](v8.15.4.md) | 2026-01-05 | Browser URL recovery revert, dashboard improvements, and text editor dependency updates |
| [v8.15.3](v8.15.3.md) | 2026-01-05 | Fixed browser URL recovery crash caused by stale object reference |
| [v8.15.2](v8.15.2.md) | 2026-01-05 | Per-window project isolation and panel mapping fixes |
| [v8.15.1](v8.15.1.md) | 2026-01-05 | Added {claudeContinueFlag} placeholder for conditional Claude Code continuation flags in workspace configurations |
| [v8.15.0](v8.15.0.md) | 2026-01-02 | BOSS-branded Chromium support and complete LSP integration for BossEditor |
| [v8.13.31](v8.13.31.md) | 2025-12 | Terminal links open in BOSS browser, Linux browser session persistence fix, and enhanced Linux package installation |
| [v8.13.30](v8.13.30.md) | 2025-12 | Terminal tab title sync, Linux dock icon fix, configurable title bar, and macOS menu hang fix |
| [v8.13.29](v8.13.29.md) | 2025-12 | Fixed missing packaging dependencies for ARM64 Linux builds and updated BossTerm to 1.0.48 |
| [v8.13.28](v8.13.28.md) | 2025-12 | Terminal tab management with state persistence, background color fixes, and dependency updates (Kotlin 2.3.0, Logback 1.5.23) |
| [v8.13.27](v8.13.27.md) | 2025-12 | JxBrowser update to 8.15.0 with version consistency fixes and banner styling improvements |
| [v8.13.26](v8.13.26.md) | 2025-12 | Terminal migration to BossTerm library, ARM64 Linux support, native binary signing for macOS, and window maximized by default |
| [v8.13.23](v8.13.23.md) | 2024-12 | Dependency updates including Clikt 5.0, JUnit 6.0, Kotlin 2.3.0-RC, and Ktor 3.3.2 |
| [v8.13.22](v8.13.22.md) | 2024-12 | Window management enhancements with title bar double-click maximize and panel minimum size constraints |
| [v8.13.21](v8.13.21.md) | 2024-12 | Fixed critical JxBrowser 'closed object' crash with thread-safe disposal mechanism |
| [v8.13.20](v8.13.20.md) | 2024-12 | Download management with auto-closing redirect tabs and Kotlin compiler deprecation warnings resolved |
| [v8.13.19](v8.13.19.md) | 2024-12 | Gradle configuration cache compatibility enabled for 10-50% faster builds with proper task input/output tracking |
| [v8.12.34](v8.12.34.md) | 2025-12 | Workspace management and tab navigation improvements: fresh workspace for new windows, auto-scroll to active tab, and auto-show New Tab Dialog |
| [v8.12.33](v8.12.33.md) | 2025-12 | Browser reliability and performance improvements with favicon caching and JxBrowser initialization retry mechanism |
| [v8.12.32](v8.12.32.md) | 2025-12 | Browser usability improvements with enhanced context menu, better URL bar interaction, and improved window focus handling |
| [v8.12.20](v8.12.20.md) | 2024-12 | Fixed critical version downgrade bug with build system improvements and CI/CD verification |
<!-- RELEASE_INDEX_END -->

## Commit Conventions

| Type | Description |
|------|-------------|
| `feat:` | New feature |
| `fix:` | Bug fix |
| `refactor:` | Code refactoring |
| `perf:` | Performance improvement |
| `docs:` | Documentation |
| `deps:` | Dependency updates |

Scopes: `browser`, `terminal`, `tabs`, `panel`, `workflow`, etc.

---

*Release notes are automatically generated using [Claude Code](https://claude.ai/claude-code).*
